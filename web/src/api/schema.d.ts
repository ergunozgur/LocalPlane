/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/v1/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Status
         * @description Backend liveness only. Says nothing about whether the host can be seen.
         */
        get: operations["get_status_api_v1_status_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/host": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Host */
        get: operations["get_host_api_v1_host_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/agent": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Agent
         * @description Probe the agent. An unreachable agent is a 200 that says so, not a 500.
         *
         *     "The agent is not answering" is a true and useful answer about the state of the
         *     system, and a caller should not have to interpret a transport error to learn it.
         */
        get: operations["get_agent_api_v1_agent_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/agent/capabilities": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Agent Capabilities
         * @description What the agent can actually do, re-probed if it is answering.
         *
         *     A capability absent from this list is not available. A capability listed
         *     ``unavailable`` is not available either, and ``reason`` says why.
         */
        get: operations["get_agent_capabilities_api_v1_agent_capabilities_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List Network Interfaces
         * @description Every network interface object LocalPlane has recorded on this host.
         *
         *     Reads the store. It does not observe: what comes back is what was last seen, with the
         *     freshness of each observation and the sweep it came from attached, so a caller can
         *     tell current truth from remembered truth.
         */
        get: operations["list_network_interfaces_api_v1_network_interfaces_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Network Interface */
        get: operations["get_network_interface_api_v1_network_interfaces__object_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/protection": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Network Interface Protection
         * @description Whether this interface is protected, why, and on what evidence.
         *
         *     Pure: derived for this request from evidence LocalPlane already holds, contacting
         *     nothing and writing nothing.
         *
         *     **A separate axis from ownership.** Ownership asks whose this object is; protection asks
         *     what changing it would put at risk. An interface can be LocalPlane's own and protected,
         *     or externally configured and not protected at all, and neither answer implies the other
         *     — a Run against a Docker-configured bridge can truthfully publish `protection: clear`
         *     while remaining blocked because Docker demonstrably configures it.
         *
         *     One protection reason is implemented: the management path. `implemented_reasons` says
         *     so out loud, because `clear` means "these were evaluated and none applied" and a reader
         *     is entitled to know what "these" were.
         *
         *     The answer depends on the connection asking. Read over a transport that cannot establish
         *     a management path — from loopback, or through a reverse proxy — every interface is
         *     `unknown`, including the ones obviously not carrying it. Marking the rest clear while
         *     the path itself is unresolved would be inventing a negative from an absence.
         */
        get: operations["get_network_interface_protection_api_v1_network_interfaces__object_id__protection_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/evidence": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Network Interface Evidence
         * @description The raw sysfs values and netlink objects the newest observation was derived from.
         *
         *     A separate resource because it is large and rarely wanted — but it is available,
         *     because a claim about an operator's host that cannot be checked is worth less than one
         *     that can.
         */
        get: operations["get_network_interface_evidence_api_v1_network_interfaces__object_id__evidence_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/provenance": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Network Interface Provenance
         * @description Who made this interface, who configures it, and the evidence for both.
         *
         *     A separate resource from the interface because the evidence is heavy: the exact Docker
         *     network a bridge's address belongs to, the NetworkManager connection bound to a device,
         *     the daemon addresses that matched. The interface itself carries the conclusion and the
         *     reason codes; this carries what they rest on, plus every source that was consulted —
         *     including the ones that answered "nothing to do with me", because a settled question
         *     and an unexamined one are different states and both are worth seeing.
         *
         *     Computed for this request. Ownership is never stored: it is a function of the newest
         *     observation and the newest provider readings, and a recorded verdict would be a second
         *     copy of something that moves whenever either does.
         */
        get: operations["get_network_interface_provenance_api_v1_network_interfaces__object_id__provenance_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/observations/refresh": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Refresh Network Observations
         * @description Observe the host's network interfaces now and take ownership of the result.
         *
         *     Read-only with respect to the host: this reads sysfs and rtnetlink and writes only
         *     LocalPlane's own store. No interface is created, modified or brought up or down.
         */
        post: operations["refresh_network_observations_api_v1_network_observations_refresh_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/systemd/units": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List Systemd Units
         * @description Stored loaded-unit observations.  Pure: this never contacts systemd or the agent.
         */
        get: operations["list_systemd_units_api_v1_systemd_units_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/systemd/units/{object_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Systemd Unit
         * @description One stored unit by LocalPlane object id, never by caller-supplied unit name.
         */
        get: operations["get_systemd_unit_api_v1_systemd_units__object_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/systemd/observations/refresh": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Refresh Systemd Observations
         * @description Read the fixed loaded-unit scope and store it.  No body, filter or D-Bus fields.
         */
        post: operations["refresh_systemd_observations_api_v1_systemd_observations_refresh_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/docker/containers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List Docker Containers
         * @description Every container LocalPlane has observed on this host, newest observation each.
         *
         *     Pure: this reads what the last sweep recorded and contacts neither the agent nor the
         *     Docker daemon. `POST /docker/containers/observations/refresh` is how a fresh reading is
         *     taken, exactly as it is for interfaces.
         *
         *     Docker is authoritative for all of this and LocalPlane keeps no copy it maintains — what
         *     is here is the newest observation, with its own freshness, of what the daemon said.
         */
        get: operations["list_docker_containers_api_v1_docker_containers_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/docker/containers/{object_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Docker Container
         * @description One container: what it is, what it is doing, and what it is attached to.
         *
         *     Enough that an operator does not need `docker ps` or `docker inspect` for routine
         *     inspection — image and digest, creation and start times, restart policy, health, port
         *     bindings, mounts, attached networks and the labels worth operating on.
         */
        get: operations["get_docker_container_api_v1_docker_containers__object_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/docker/containers/observations/refresh": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Refresh Container Observations
         * @description Ask the Docker daemon about every container on this host now, and record the result.
         *
         *     Read-only with respect to the host: this lists and inspects, and there is no method on
         *     the path it takes that could start, stop, create or remove anything. No body and no
         *     parameter — there is nothing to select, because the sweep is of the estate.
         */
        post: operations["refresh_container_observations_api_v1_docker_containers_observations_refresh_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/docker/containers/{object_id}/logs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Read Container Logs
         * @description The most recent lines this container wrote.
         *
         *     `POST` rather than `GET` for the reason every observation in this API is a `POST`: it
         *     contacts the host. LocalPlane stores no copy of a container's output — Docker keeps the
         *     logs, and building a second copy of them would be a log platform rather than a control
         *     plane. Read-only with respect to the host, and bounded by lines and by bytes.
         */
        post: operations["read_container_logs_api_v1_docker_containers__object_id__logs_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/docker/containers/{object_id}/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Read Container Stats
         * @description One current sample of what this container is using.
         *
         *     A snapshot, and deliberately only a snapshot: there is no history, no series and no
         *     retention, because a metrics database is a product this build does not have and a field
         *     on a container is not the place to grow one. Read-only with respect to the host, and a
         *     `POST` for the same reason the log read is.
         */
        post: operations["read_container_stats_api_v1_docker_containers__object_id__stats_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/management-path/observations/refresh": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Refresh Management Path
         * @description Observe how *this* request reached LocalPlane, and record what the kernel says.
         *
         *     **There is no request body and no parameter, and that is the design.** The peer and the
         *     local endpoint come from the server side of this connection — the accepted socket — and
         *     the route lookup's destination is the peer LocalPlane read there. A caller cannot name a
         *     peer address, a local address, an interface, a route target, a command or an argv,
         *     because there is nothing here to name them to. `X-Forwarded-For`, `X-Real-IP` and
         *     `Forwarded` are read by nothing on this path, and no setting turns them on: a management
         *     path proven from something the caller wrote is a request that has been believed rather
         *     than evidence that has been gathered.
         *
         *     **Read-only with respect to the host.** The one thing this contacts is the kernel's
         *     routing table, through a typed agent method that sends `RTM_GETROUTE` and can send
         *     nothing else. No interface, address, route, rule or service is created, modified or
         *     removed: this path reaches none of the agent's mutating methods, which belong to the
         *     apply of a Run and to nothing here.
         *
         *     **A request that cannot prove anything records nothing.** From loopback, from a reverse
         *     proxy on this host, or over a transport whose endpoints cannot be read, the answer is
         *     `unresolved` with the reason saying which, and no row is written — a record that looks
         *     like evidence, sorts as the newest evidence and proves nothing is worse than none.
         *
         *     Confirming the path needs two independent sources to agree: the local address this
         *     connection terminated on must be present on exactly one currently observed object, and
         *     the kernel's route to the peer must leave by that same object. Where they disagree the
         *     answer is `unresolved`, because choosing between two disagreeing sources is how a
         *     control plane reaches a confident wrong answer.
         */
        post: operations["refresh_management_path_api_v1_management_path_observations_refresh_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/management-path": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Management Path
         * @description Which object carries the connection this request arrived on, from what is recorded.
         *
         *     Pure. It contacts no agent, queries no kernel, refreshes no evidence and writes nothing
         *     — not a row, not a timestamp. Reading where you are connected from must not be able to
         *     change what LocalPlane has recorded, or the record stops being a record of the host and
         *     becomes a record of who was looking.
         *
         *     The answer depends on the connection asking, deliberately. Evidence taken from one
         *     operator's session answers for that session and no other: not for a second operator, not
         *     for automation calling over loopback, and not for the same operator arriving on a
         *     different address. Where nothing recorded matches this connection, the answer is
         *     `unresolved` with `management_path_unobserved`, and the remedy is to observe.
         */
        get: operations["get_management_path_api_v1_management_path_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/observations/sweeps": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List Sweeps */
        get: operations["list_sweeps_api_v1_observations_sweeps_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/adopt": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Adopt Network Interface
         * @description Adopt this interface: ``observed → managed``.
         *
         *     Records the currently verified values of the fields LocalPlane can be answerable for —
         *     ``admin_up`` and ``mtu`` — as a new intent version, and starts comparing against them.
         *
         *     **This does not change the host.** Adoption is LocalPlane agreeing to notice, not
         *     LocalPlane taking control: the values it stores are the ones that were already there.
         *     It is refused rather than approximated when the object is observe-only, when it is
         *     already managed, when the newest observation is too old, when a value it would control
         *     could not be read — or when another system is demonstrably running the object.
         *
         *     That last refusal is what the ownership axis is for. A bridge the Docker daemon created
         *     and configures, an interface NetworkManager holds an active profile for: LocalPlane has
         *     no write model that could share either of them, so it declines to become answerable
         *     rather than retaining an intent it could not honour. The refusal names the owner and
         *     the evidence.
         */
        post: operations["adopt_network_interface_api_v1_network_interfaces__object_id__adopt_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/release": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Release Network Interface
         * @description Release this interface: ``managed → observed``.
         *
         *     Drops the retained intent so LocalPlane stops being answerable for the object, and
         *     resolves any drift it had open about it — because a claim that the runtime disagrees
         *     with the intent stops being true when there is no intent.
         *
         *     **This does not change the host, and it is not a rollback.** Nothing is restored,
         *     because nothing was ever applied. An interface released while it is down stays down.
         *     Every intent version is retained as history.
         */
        post: operations["release_network_interface_api_v1_network_interfaces__object_id__release_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/intent/revise": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revise Network Interface Intent
         * @description Retain new desired values for an interface LocalPlane already manages.
         *
         *     This is the operator saying what they now want. It writes a **new immutable intent
         *     version**, moves the active-intent pointer to it and leaves the version it replaced
         *     exactly as it was — the whole chain stays readable, so what was intended at any point
         *     and why it stopped being intended are both still answerable.
         *
         *     **This does not change the host, and it is not an apply.** An MTU revised to 1400 is an
         *     MTU nobody has set; if the interface already carries 1400 the two now agree, and they
         *     agree because the intent moved. That is the second truthful answer to drift, and it is
         *     the answer LocalPlane uses when the machine is right and the declaration is out of date.
         *
         *     Management does not move: the object was ``managed`` before and is ``managed`` after.
         *     Only fields the active intent already controls may be given a value — widening what
         *     LocalPlane is answerable for is an adoption decision, made against verified evidence,
         *     not something a request body may do. A field left out keeps the value it had.
         *
         *     Refused rather than approximated when the object is not managed, when the version named
         *     in ``expected_intent_id`` is no longer in force, when the newest observation is too old
         *     to have decided against, when a name is not one LocalPlane controls, when a value is of
         *     the wrong type, when the request would retain exactly what is already retained — or
         *     when another system is demonstrably running the object, which is the same gate adopt
         *     applies and for the same reason.
         */
        post: operations["revise_network_interface_intent_api_v1_network_interfaces__object_id__intent_revise_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/intent/adopt-runtime": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Adopt Runtime As Network Interface Intent
         * @description Make what this interface currently has the retained desired state.
         *
         *     A separate endpoint from ``revise`` on purpose. The two are different acts — one is a
         *     statement about what should be, the other a statement that what *is* was right all
         *     along — and folding them into one request with a mode flag would leave a history in
         *     which nobody could tell afterwards which had happened.
         *
         *     Only currently verified values are taken, and only for the fields the active intent
         *     already controls. A controlled field the newest observation could not read refuses the
         *     whole revision: "adopt what is there" cannot be said about a value nobody could see,
         *     and filling it in from the version being replaced would be inventing agreement.
         *
         *     **This does not change the host.** Every value recorded here was read from it, which is
         *     why the result is ``in_sync`` and why nothing needed applying to get there. Any drift
         *     this settles is resolved as ``intent_revised`` — never as though the runtime had been
         *     put right.
         */
        post: operations["adopt_runtime_as_network_interface_intent_api_v1_network_interfaces__object_id__intent_adopt_runtime_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/intent": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Network Interface Intent
         * @description The intent currently in force for this interface.
         *
         *     404 when there is none. That is not an error condition — an observed object is a
         *     perfectly healthy thing to be — but "no intent" and "an intent controlling nothing"
         *     are different answers and this resource only has one of them to give.
         */
        get: operations["get_network_interface_intent_api_v1_network_interfaces__object_id__intent_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/intent/history": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Network Interface Intent History
         * @description Every intent version ever retained for this interface, and every transition.
         *
         *     Release does not delete anything, so a released object still answers here with what it
         *     was managed as, and the transition that ended it.
         */
        get: operations["get_network_interface_intent_history_api_v1_network_interfaces__object_id__intent_history_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/network/interfaces/{object_id}/reconciliation": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Network Interface Reconciliation
         * @description Compare this interface's retained intent with its newest observation.
         *
         *     Computed for this request, not read from a column. An object that is not managed
         *     answers ``reconciliation: null`` with a 200: "there is nothing to compare" is the
         *     correct answer, and it is not the same as ``in_sync``.
         */
        get: operations["get_network_interface_reconciliation_api_v1_network_interfaces__object_id__reconciliation_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/findings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List Findings
         * @description LocalPlane's claims about this host.
         *
         *     A finding is an interpretation, and it is not the same thing as a state: an object's
         *     ``reconciliation`` is recomputed on every read, while a finding remembers when the
         *     disagreement started and survives being resolved. Re-observing the same disagreement
         *     updates one finding rather than adding another, and a finding that stops being true is
         *     resolved with the reason — never deleted.
         *
         *     Two kinds are reported here, and ``evidence`` is shaped by ``finding_type``. Drift is a
         *     managed object disagreeing with its retained intent. An ownership conflict is a managed
         *     object another system is demonstrably running — which is a safety claim rather than a
         *     configuration one, and is never raised for an object LocalPlane merely watches.
         */
        get: operations["list_findings_api_v1_findings_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/findings/{finding_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get Finding */
        get: operations["get_finding_api_v1_findings__finding_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List Runs
         * @description Every Run on this host, newest first.
         *
         *     None of them is a Change. Nothing here has written to the host, and the `validity` on
         *     each says whether the plan it published still describes what would happen.
         */
        get: operations["list_runs_api_v1_runs_get"];
        put?: never;
        /**
         * Create Run
         * @description Plan one typed operation and publish its preview.
         *
         *     **This endpoint does not change the host.** What it creates is a Run — the durable
         *     record that somebody asked what an operation would involve — together with an immutable
         *     preview answering it. Confirmation and apply remain separate generic endpoints. The
         *     systemd operations have a registered executor like the container operations do, so an
         *     eligible one proceeds through those endpoints to a real host write; planning here
         *     reaches none of it.
         *
         *     The preview answers seven questions as precisely as current truth allows: what would
         *     change, why, how it would run, what evidence it rests on, what it risks, what would have
         *     to be verified afterwards and what could be recovered. Where LocalPlane does not know,
         *     it says `unknown` — most importantly about whether this interface carries the path the
         *     operator is reaching it over, which nothing in the current observation model can prove
         *     and which is therefore never guessed.
         *
         *     **A preview can exist while execution is blocked, and that is the useful case.** An
         *     interface Docker configures still gets a plan saying "MTU 1400 → 1500", with the
         *     ownership conflict listed as a blocker. Refusing to describe it would be less
         *     informative and no safer.
         *
         *     Planning is refused, with nothing written, when there is no honest plan to make: the
         *     object is not managed, its active intent does not control the field, nobody has
         *     observed it recently enough, the current value cannot be read — or the runtime already
         *     carries the intended value, which is a truthful no-op rather than a plan that would
         *     change nothing.
         *
         *     The desired value is never taken from this request. It comes from the retained intent,
         *     and an operator who wants a different one revises the intent first.
         */
        post: operations["create_run_api_v1_runs_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs/{run_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Run
         * @description One Run and the plan it published.
         *
         *     Pure. Reading a Run does not contact the host, does not refresh an observation, does
         *     not re-plan the stored preview and does not move a timestamp because somebody looked.
         *     `preview.validity` *is* derived for this request — from records LocalPlane already had,
         *     and writing nothing — because whether a plan still holds is a question about now.
         */
        get: operations["get_run_api_v1_runs__run_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs/{run_id}/preview": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Run Preview
         * @description The immutable plan this Run published.
         *
         *     This is the document a future confirmation would be checked against: its
         *     `preview_digest` is a hash of a canonical form of the plan, so "the operator confirmed
         *     this" can one day be verified rather than assumed. It is stored, never rewritten — a
         *     trigger in the store refuses every update — so a stale plan stays readable exactly as
         *     it was published, and the remedy for staleness is a new Run.
         */
        get: operations["get_run_preview_api_v1_runs__run_id__preview_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs/{run_id}/confirm": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Confirm Run
         * @description Satisfy the confirmation this run's published plan requires.
         *
         *     **The body names the preview, not just a digest.** Two identical concurrent plans share
         *     one digest, so a confirmation keyed on content could not say which of them an operator
         *     looked at, and a confirmation for one run must never authorise another. `preview_id` is
         *     required and must be the one this run published; a digest may be supplied as an
         *     optional cross-check.
         *
         *     **Nothing is issued.** No token comes back and none is stored. The confirmation is a row
         *     naming this run and this plan, it is single-use, and it is consumed atomically by an
         *     apply of *this* run and by nothing else.
         *
         *     **Nobody is identified.** LocalPlane has no authentication. The record says what is
         *     true — that an unauthenticated request satisfied the requirement — rather than inventing
         *     an actor to attribute it to.
         *
         *     A stale plan cannot be confirmed, and neither can a blocked one. Confirming work that
         *     could not proceed is how blocked work acquires the means to reach an apply review.
         *
         *     Nothing about the host is touched here, and no Change is created: confirming is a
         *     decision about LocalPlane's own records, exactly as adopting and revising are.
         */
        post: operations["confirm_run_api_v1_runs__run_id__confirm_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs/{run_id}/self-impact-override": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Grant Self Impact Override
         * @description Accept that carrying this plan out may interrupt LocalPlane itself.
         *
         *     **A second, separate authority, and it is not a confirmation.** Confirming answers "do
         *     you want this change". This answers a different question about a different fact: this
         *     plan's effect closure reaches the infrastructure LocalPlane's own backend depends on, so
         *     executing it may take LocalPlane away — temporarily for a restart, indefinitely for a
         *     stop — and nothing here has verified another way to reach this host. Both are required,
         *     neither substitutes for the other, and the write boundary demands each through its own
         *     database trigger.
         *
         *     **It is offered for one hazard and one shape of evidence.** The plan's `how.eligibility`
         *     must be `self_impact_override_required`, which a plan reaches only where its self-impact
         *     derivation is `proven` under `docker-direct-unix-v1`, the closure touches the management
         *     path through the backend's runtime owner and nothing else, the agent is proven to be a
         *     host-side service outside that closure, and no gap remains. A `possible` impact is
         *     surfaced and never authorised. An unresolved one, an agent in a container, an incomplete
         *     effect graph, a provider whose owner could not be resolved, an applicability failure, a
         *     stale preview, a digest mismatch or any blocker a later build adds all leave the plan
         *     `blocked` — and a blocked plan has no override to grant.
         *
         *     **There is nothing to force and nothing to name.** No `force`, no `ignore_safety`, no
         *     list of blockers to bypass: a caller cannot choose what is being overridden, because the
         *     backend derived whether this exact published plan is eligible for this one authority.
         *     Nor is any display text echoed back — everything the acknowledgement is about is in the
         *     immutable preview this request names, bound by the digest it optionally cross-checks.
         *
         *     **Protection does not move.** `protected` stays `protected` and `unknown` stays
         *     `unknown`; every blocker stays published; the risk tier and the confirmation requirement
         *     are untouched. Granting this opens a path, it does not soften a verdict.
         *
         *     **It is single-use and bound to one plan.** One grant per run, ever; it names the
         *     preview that run published; it is re-validated against fresh evidence when granted and
         *     again when spent; and it is consumed atomically with the apply confirmation by the one
         *     attempt that uses it. Nothing is issued and no token comes back.
         */
        post: operations["grant_self_impact_override_api_v1_runs__run_id__self_impact_override_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs/{run_id}/apply": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Apply Run
         * @description Execute this run's published plan. One of the two endpoints that can write.
         *
         *     The other is `POST /changes/{id}/recovery/retry`, which re-attempts the end state of a
         *     change that could not be settled. Both dispatch through the same executors, and neither
         *     takes a request body.
         *
         *     **There is no request body, and that is the design.** Every authoritative value is
         *     already named by the run: the object, the operation, the intent version, the field and
         *     both ends of the change. There is no parameter here for an MTU, a desired value, an
         *     interface, an object id, a command, an argv, an executable, a provider, a shell, a
         *     patch or a field set — not because they are validated away, but because there is
         *     nothing to name them to. An operator who wants a different value revises the intent and
         *     plans a new run.
         *
         *     **Everything is re-proved for this request before anything is armed.** The object is
         *     re-read, the published preview must itself say execution was available and this plan
         *     eligible, its validity is re-derived under *this* request's management-path evidence,
         *     and every execution gate — ownership, protection, capability, and that the target is
         *     proven **not** to be the path this request arrived over — is checked again against
         *     current truth. Nothing is silently replanned: the remedy for a stale run is a new run.
         *
         *     **A second concurrent apply against the same object and field is refused**, with a typed
         *     conflict, by a durable lock in the database rather than a mutex in one process.
         *
         *     **The order after that is the safety argument.** The confirmation is consumed
         *     atomically; a checkpoint holding the value to restore is written and committed *before*
         *     anything else can be; only then does a Change come into existence; and only then is the
         *     mutation dispatched. A checkpoint that cannot be written ends the run `failed` with no
         *     Change, because nothing about the host could have moved.
         *
         *     **What comes back is what is known.** `change.mutation.outcome` is `not_written`,
         *     `written` or `write_unknown`, and the third is never converted into either of the others
         *     by reading the value back — that answers what the host holds now, which is a different
         *     question from whether this write occurred. A `written` mutation is verified by a fresh
         *     observation through the ordinary path before the run can succeed, and a verification
         *     that cannot prove the intended value sends the run to restoration. A restoration that
         *     cannot be proven ends in `recovery_required`, which is a truthful ending rather than an
         *     error, and holds this object's write lock until somebody looks.
         */
        post: operations["apply_run_api_v1_runs__run_id__apply_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs/{run_id}/guard/keep": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Keep Guarded Run
         * @description Keep a guarded change, by proving over the changed object that you are still here.
         *
         *     **The proof is this request.** A guarded change is one made to the object carrying the
         *     operator's own management path, and nothing LocalPlane can read from the host settles
         *     whether that path survived it — the value comes back perfectly over a link nobody can
         *     talk to. What settles it is a request that *arrived*, whose own transport re-establishes
         *     the management path by the same two-source rule the path is always proven by and
         *     resolves it to the very object that was changed. That evidence travels over the path
         *     under test, which is what makes it a proof.
         *
         *     So a request that cannot prove it can never keep the change — not because it is refused
         *     a permission, but because the one thing it would have to establish is the one thing it
         *     has failed to establish. It is refused with `guard_connection_not_proved` and the guard
         *     goes on holding.
         *
         *     **This endpoint cannot write to the host**, and there is no parameter through which it
         *     could. The only body field is a statement of intent; it names no object, no value, no
         *     interface, no route, no command and no deadline. What it can do is *prevent* a write:
         *     releasing the guard is what stops the reversal that would otherwise happen.
         *
         *     **Doing nothing is a decision, and it is the safe one.** If the window passes with no
         *     such proof, the agent on the host dispatches the reversal on its own — without this
         *     process, without this connection and without being asked again. Calling this afterwards
         *     does not undo that; it collects what the guard did, reads the target back, and ends the
         *     run truthfully: `rolled_back` where a reading proves the previous value is back,
         *     `recovery_required` where nothing does.
         */
        post: operations["keep_guarded_run_api_v1_runs__run_id__guard_keep_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/runs/{run_id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel Run
         * @description Cancel a Run before the write boundary.
         *
         *     The easy cancellation, and the only one that exists: nothing has been written, so
         *     nothing has to be put back. **No Change is created** — LocalPlane's first invariant is that a
         *     cancelled run carries no change record, and here there is not even a table one could
         *     live in. The host is untouched, the retained intent is untouched, reconciliation and
         *     findings are untouched, and the published preview remains inspectable as the history it
         *     is.
         *
         *     This says nothing about cancelling an execution. Cancelling during an apply means the
         *     effect of the interrupted step is unknown and the run goes to recovery; cancelling
         *     during a rollback is not a rollback. Neither is designed here, and neither is
         *     reachable — there is no state to interrupt.
         */
        post: operations["cancel_run_api_v1_runs__run_id__cancel_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/changes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List Changes
         * @description Every Change on this host, newest first. Pure: reading history changes nothing.
         */
        get: operations["list_changes_api_v1_changes_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/changes/{change_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Change
         * @description One Change, its apply transcript, and what LocalPlane does and does not know.
         *
         *     Pure. Reading a Change contacts no host, refreshes no observation and writes nothing —
         *     including for a Change that ended in `recovery_required`, where the temptation to "check
         *     whether it is fine now" is exactly the read that must not silently become a write.
         */
        get: operations["get_change_api_v1_changes__change_id__get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/changes/{change_id}/recovery/confirm": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Confirm Change Recovery
         * @description Authorise one recovery retry of this change to write to the host again.
         *
         *     **The confirmation that authorised the original apply is not reusable and this is not
         *     it.** That one authorised an attempt and the attempt happened. This is a second, separate
         *     grant, recorded in the same table under the same single-use rule, consumed by exactly one
         *     retry — and at most one may be outstanding at a time, so authority cannot be accumulated.
         *
         *     **A retry does not need this to begin.** The first thing a retry does is take a fresh
         *     reading and ask the operation whether it already proves the end state; one that does
         *     completes with no host mutation and consumes nothing. Grant this when a retry has told you
         *     it cannot proceed without it, or in advance if you already know the write must happen.
         *
         *     The current management-path gate applies here as it does to the retry itself: authority
         *     over a write that could remove the operator's own path is granted over a connection that
         *     has proven it is not the one at risk, or it is not granted.
         *
         *     Nothing about the host is touched, and no Change is created or altered.
         */
        post: operations["confirm_change_recovery_api_v1_changes__change_id__recovery_confirm_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/changes/{change_id}/recovery/retry": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Retry Change Recovery
         * @description Try again to reach the end state this change wanted. The way out of a recovery hold.
         *
         *     **There is no request body, and that is the design.** Every authoritative value is
         *     already named by the change: the operation, the object, the field and both ends of it, or
         *     the verb and the state it promised. There is no parameter here for a value, a verb, a
         *     target, a provider, a command or a shell — not because they are validated away, but
         *     because there is nothing to name them to. A retry re-attempts *this* change; an operator
         *     who wants something else plans a new run.
         *
         *     **It looks before it writes.** The first thing it does is re-read the target through the
         *     ordinary observation path and ask the operation whether that reading already proves the
         *     end state — an uncertain write may in fact have landed, a container may now be proven
         *     running, a restart may now be provable from the daemon's own record of when it last
         *     started. If it is proven, recovery completes with **no host mutation at all** and the
         *     object is given back. Writing again merely because somebody pressed retry would be a host
         *     change nobody needed.
         *
         *     **Everything is re-proved for this request.** The management-path judgement comes from the
         *     transport this call arrived on, not from the failed run's; ownership, capability,
         *     observation currency and eligibility are re-derived against current truth; and a re-plan
         *     that would now produce a different desired value, verb or expected state is **refused**,
         *     because recovery may not substitute today's intent for the one the change was for.
         *
         *     **A retry that must write needs authority nobody has spent.** It is refused with
         *     `recovery_confirmation_required` until one is granted, and the refusal is recorded as an
         *     attempt that provably wrote nothing.
         *
         *     **The hold is kept through every ending but two.** `not_written`, `write_unknown` and a
         *     write that could not be proven all leave the question open, so the object stays held. The
         *     original change is never rewritten by any of this: it goes on saying it required recovery
         *     and goes on saying why.
         */
        post: operations["retry_change_recovery_api_v1_changes__change_id__recovery_retry_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/changes/{change_id}/recovery/resolve": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Resolve Change Recovery
         * @description Release this change's recovery hold by hand. **No host mutation happens here.**
         *
         *     The explicit human escape, and the store enforces what it may be: a resolution row may
         *     carry no confirmation, no dispatch, no mutation outcome, no host effect and no
         *     verification, so a build that decided otherwise would fail to write it.
         *
         *     **It claims nothing.** Not that the change succeeded, not that the mutation happened, not
         *     that the host is safe, not that anything was rolled back, not that the intended state was
         *     reached. The original result and the original recovery reason are left saying exactly what
         *     they said and stay inspectable afterwards; what is added is that at this time a person
         *     released the hold.
         *
         *     **Whatever can be observed is recorded beside it**, through the ordinary observation path,
         *     and recorded as what it is. If the reading happens to prove the end state the change
         *     wanted, the record says so and the outcome is still `resolved` — a human's decision is not
         *     silently upgraded into a verification. If nothing can be read, that stays visible rather
         *     than becoming an absence a reader could mistake for a clean result.
         *
         *     `acknowledge_object` is the object's name, typed out. The operator types the held
         *     domain's name for the same reason: an escape from a safety hold should not be one
         *     accidental click, and the statement is recorded rather than merely required.
         *
         *     Only the lock this change holds is released, and only while this change still holds it.
         */
        post: operations["resolve_change_recovery_api_v1_changes__change_id__recovery_resolve_post"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** Address */
        Address: {
            /** Family */
            family: string;
            /** Address */
            address: string;
            /** Prefix Length */
            prefix_length: number;
            /** Scope */
            scope: string | null;
            /** Dynamic */
            dynamic: boolean | null;
            /** Valid Lifetime S */
            valid_lifetime_s: number | null;
            /** Preferred Lifetime S */
            preferred_lifetime_s: number | null;
        };
        /**
         * AdoptionEligibility
         * @description Whether LocalPlane would take responsibility for this object, and why not if not.
         *
         *     A separate axis from both management state and ownership. Management is where the
         *     object is; ownership is what is true about it; this is the policy that reads both.
         */
        AdoptionEligibility: {
            /** Eligible */
            eligible: boolean;
            /**
             * Reason
             * @description object_observe_only | already_managed | externally_configured | externally_created | conflicting_ownership_claims | no_external_owner_proven
             */
            reason: string;
            /** @description The system whose ownership makes this ineligible. */
            blocked_by?: components["schemas"]["Owner"] | null;
            /**
             * Evidence Gaps
             * @description Sources that left the question open. Never makes an object eligible, and does not by itself make one ineligible — adoption records values that are already true and writes nothing. Reported so that an unexamined source is not mistaken for a clean one.
             */
            evidence_gaps?: string[];
        };
        /** AgentIdentity */
        AgentIdentity: {
            /**
             * Agent Instance Id
             * @description Identifies an agent *process*. A restart produces a new one.
             */
            agent_instance_id: string;
            /** Agent Version */
            agent_version: string;
            /** Protocol Version */
            protocol_version: string;
            /** Transport */
            transport: string;
            /** Process Isolated */
            process_isolated: boolean;
            /**
             * Privilege
             * @description The privilege the agent process actually holds.
             */
            privilege: string;
            /** Effective Uid */
            effective_uid: number | null;
            /** Pid */
            pid: number | null;
            /**
             * Started At
             * Format: date-time
             */
            started_at: string;
            /**
             * Last Contact At
             * Format: date-time
             */
            last_contact_at: string;
        };
        /**
         * AgentStatus
         * @description Whether the agent is answering *now*, and what was last recorded about it.
         */
        AgentStatus: {
            /** Reachable */
            reachable: boolean;
            /**
             * Source
             * @description live — probed for this request; recorded — last known.
             */
            source: string;
            /**
             * As Of
             * Format: date-time
             */
            as_of: string;
            /** @description Why the agent could not be reached, when it could not. */
            error?: components["schemas"]["ErrorBody"] | null;
            agent: components["schemas"]["AgentIdentity"] | null;
            /** Socket */
            socket: string;
        };
        /**
         * BackendStatus
         * @description Backend liveness. Deliberately independent of the agent.
         *
         *     This answers "is the backend up", which is a different question from "can LocalPlane
         *     see the host". The second question is what ``/agent`` is for, and answering both here
         *     would make a reachable backend look like a working control plane.
         */
        BackendStatus: {
            /**
             * Status
             * @default ok
             */
            status: string;
            /** Version */
            version: string;
            database: components["schemas"]["DatabaseStatus"];
        };
        /** Capabilities */
        Capabilities: {
            /** Reachable */
            reachable: boolean;
            /** Source */
            source: string;
            /**
             * As Of
             * Format: date-time
             */
            as_of: string;
            /** Agent Instance Id */
            agent_instance_id: string | null;
            error?: components["schemas"]["ErrorBody"] | null;
            /** Capabilities */
            capabilities: components["schemas"]["Capability"][];
        };
        /** Capability */
        Capability: {
            /** Capability */
            capability: string;
            /** Version */
            version: number;
            /**
             * Status
             * @description available | degraded | unavailable
             */
            status: string;
            /** Mutating */
            mutating: boolean;
            /** Summary */
            summary: string;
            /** Reason */
            reason: string | null;
            /** Detail */
            detail: {
                [key: string]: unknown;
            };
            /**
             * Discovered At
             * Format: date-time
             */
            discovered_at: string;
        };
        /**
         * Change
         * @description The durable record that LocalPlane entered a path on which a host write may occur.
         *
         *     **Not a claim that one happened.** `mutation.outcome` answers that, and it has three
         *     values because there are three truths. A Change may exist with `not_written`: the
         *     boundary was crossed and the privileged path refused before the kernel could accept
         *     anything, which is a true and useful thing to have recorded.
         *
         *     A Change is also none of the other things LocalPlane records. It is not a management
         *     transition, not an intent revision, not a Run and not a confirmation — those are
         *     decisions about LocalPlane's own records, and this is the moment it stopped deciding.
         */
        Change: {
            /** Change Id */
            change_id: string;
            /** Run Id */
            run_id: string;
            /** Preview Id */
            preview_id: string;
            /**
             * Checkpoint Id
             * @description Null for an action: there is no previous value, so nothing to restore.
             */
            checkpoint_id: string | null;
            /** Host Id */
            host_id: string;
            /** Object Id */
            object_id: string;
            /** Object Name */
            object_name: string;
            /** Operation */
            operation: string;
            /**
             * Change Kind
             * @description field | action. The discriminator, and the only honest way to read the two halves below: which of them carries anything is decided here, not by probing for nulls.
             */
            change_kind: string;
            /** Field */
            field: string | null;
            /** Before Value */
            before_value: boolean | number | null;
            /** Desired Value */
            desired_value: boolean | number | null;
            /**
             * Action
             * @description The declared verb. An action only.
             */
            action: string | null;
            /**
             * Observed State
             * @description What the resource was observed in when the plan was made. An action only.
             */
            observed_state: string | null;
            /**
             * Expected State
             * @description What must hold afterwards for the action to have worked.
             */
            expected_state: string | null;
            /**
             * Created At
             * Format: date-time
             * @description The write-boundary time.
             */
            created_at: string;
            /**
             * Host Effect
             * @description none | written | write_unknown. Widened from the single value every event table in LocalPlane carried in earlier schema versions.
             */
            host_effect: string;
            /**
             * Host Mutated
             * @description True only for `written`. `write_unknown` is not a maybe-true here.
             */
            host_mutated: boolean;
            mutation: components["schemas"]["ChangeMutation"];
            verification: components["schemas"]["ChangeVerification"];
            rollback: components["schemas"]["ChangeRollback"];
            recovery: components["schemas"]["ChangeRecovery"];
            /**
             * Result
             * @description in_flight | succeeded | failed | rolled_back | recovery_required. `failed` is permitted only where nothing was written and that is provable; the store refuses anything else.
             */
            result: string;
            /** Finished At */
            finished_at: string | null;
            /** Events */
            events: components["schemas"]["RunEventView"][];
        };
        /** ChangeList */
        ChangeList: {
            /** Host Id */
            host_id: string;
            /** Count */
            count: number;
            /** Changes */
            changes: components["schemas"]["ChangeSummary"][];
        };
        /**
         * ChangeMutation
         * @description What became of one dispatched mutation. Three truths, never interchangeable.
         */
        ChangeMutation: {
            /**
             * Outcome
             * @description not_written | written | write_unknown, or null while a dispatch is unsettled. `write_unknown` is not a failure mode of the other two: it means the write may have taken effect and LocalPlane cannot prove whether it did.
             */
            outcome: string | null;
            /** Reason */
            reason: string | null;
            /** Provider */
            provider: string | null;
            /** Method */
            method: string | null;
            /** Attempt Id */
            attempt_id: string;
            /**
             * Dispatch Began At
             * @description Written and committed before the request was sent. A change that says dispatch began and records no outcome is `write_unknown` on any later reading — that is the crash window, stated rather than hidden.
             */
            dispatch_began_at: string | null;
            /** Settled At */
            settled_at: string | null;
            /** Detail */
            detail: {
                [key: string]: unknown;
            };
        };
        /**
         * ChangeRecovery
         * @description What LocalPlane knows and does not know when it could not prove a safe end state.
         *
         *     It also records what has been done about the recovery requirement. `required` says
         *     this Change ended in recovery and goes on saying so for ever; `state` says whether the
         *     hold is still held.
         */
        ChangeRecovery: {
            /** Required */
            required: boolean;
            /**
             * State
             * @description not_required | unresolved | resolved. `resolved` means the hold was released — by a retry that proved the end state, or by a person. It never means the change succeeded, and `result` is left saying exactly what it said.
             */
            state: string;
            /**
             * Reason
             * @description Typed code, never prose.
             */
            reason: string | null;
            /**
             * Known
             * @description What is established: the value before, the value wanted, what was read.
             */
            known: {
                [key: string]: unknown;
            };
            /**
             * Unknown
             * @description What is not established, stated rather than glossed over.
             */
            unknown: string[];
            /**
             * Object Write Locked
             * @description Whether this object's controlled field is still held. A recovery-required change keeps the hold: the value is unproven, and a second change against it would be building on a foundation nobody has checked.
             */
            object_write_locked: boolean;
            /** Released At */
            released_at: string | null;
            /**
             * Released By
             * @description retry | resolve, or null while it is held.
             */
            released_by: string | null;
            /** Released By Attempt Id */
            released_by_attempt_id: string | null;
            /**
             * Last Observed
             * @description The newest reading any recovery action took, and what the operation made of it. Empty until one has been taken.
             */
            last_observed: {
                [key: string]: unknown;
            };
            /**
             * Attempts
             * @description Every recovery action, in order. Append-only and never rewritten.
             */
            attempts: components["schemas"]["RecoveryAttemptView"][];
            /** @description An outstanding grant for one retry that must write again, if there is one. */
            authority: components["schemas"]["RecoveryAuthority"] | null;
            /**
             * Available Actions
             * @description What the operator can do next: `retry` re-attempts the original end state and looks before it writes; `confirm_retry` grants the authority a retry needs if it must write again; `resolve` releases the hold and claims nothing.
             */
            available_actions: string[];
        };
        /**
         * ChangeRollback
         * @description The restoration attempt, through the same privileged path, and its own read-back.
         */
        ChangeRollback: {
            /** Required */
            required: boolean;
            /** Attempt Id */
            attempt_id: string | null;
            /** Dispatch Began At */
            dispatch_began_at: string | null;
            /**
             * Outcome
             * @description not_written | written | write_unknown, or null if none was attempted.
             */
            outcome: string | null;
            /** Reason */
            reason: string | null;
            /**
             * Restores Value
             * @description The value a restoration would put back. Null for an action, which has none — the inverse of `start` is not `stop`, and the store refuses a row claiming one.
             */
            restores_value: boolean | number | null;
            verification: components["schemas"]["ChangeVerification"];
            /** Detail */
            detail: {
                [key: string]: unknown;
            };
        };
        /**
         * ChangeSummary
         * @description A Change as a list renders it.
         */
        ChangeSummary: {
            /** Change Id */
            change_id: string;
            /** Run Id */
            run_id: string;
            /** Object Id */
            object_id: string;
            /** Object Name */
            object_name: string;
            /** Operation */
            operation: string;
            /** Change Kind */
            change_kind: string;
            /** Field */
            field: string | null;
            /** Before Value */
            before_value: boolean | number | null;
            /** Desired Value */
            desired_value: boolean | number | null;
            /** Action */
            action: string | null;
            /** Expected State */
            expected_state: string | null;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /** Finished At */
            finished_at: string | null;
            /** Host Effect */
            host_effect: string;
            /** Mutation Outcome */
            mutation_outcome: string | null;
            /** Verification Outcome */
            verification_outcome: string;
            /** Rollback Outcome */
            rollback_outcome: string | null;
            /** Result */
            result: string;
            /** Recovery Required */
            recovery_required: boolean;
            /** Recovery Reason */
            recovery_reason: string | null;
            /**
             * Recovery State
             * @description not_required | unresolved | resolved. `resolved` says the hold was released, never that the change succeeded — `result` goes on saying what happened.
             */
            recovery_state: string;
        };
        /**
         * ChangeVerification
         * @description Whether a fresh reading proved the wanted value. Never the mutation's own word.
         */
        ChangeVerification: {
            /**
             * Outcome
             * @description not_attempted | verified | mismatch | value_unreadable | observation_unavailable | source_incompatible | target_absent
             */
            outcome: string;
            /**
             * Observation Id
             * @description The reading that proved it. Required for `verified`.
             */
            observation_id: string | null;
            /** Observed Value */
            observed_value: boolean | number | null;
            /**
             * Expected Value
             * @description The value a field change had to show. Null for an action, which has none.
             */
            expected_value: boolean | number | null;
            /**
             * Observed State
             * @description What an action's target was observed in. Null for a field change.
             */
            observed_state?: string | null;
            /**
             * Expected State
             * @description The state an action had to produce. Null for a field change.
             */
            expected_state?: string | null;
            /** Reason */
            reason: string | null;
        };
        /**
         * ConfirmRunRequest
         * @description Satisfy the confirmation a published plan requires.
         *
         *     ``preview_id`` is **required** and must be the preview this Run published. A digest is
         *     not enough: two identical concurrent plans share one, so a confirmation keyed on content
         *     could not say which of them an operator looked at. ``expected_preview_digest`` is an
         *     optional cross-check on *what* was confirmed, not a substitute for saying *which*.
         *
         *     Nothing else is accepted. There is no field here for an MTU, an interface, a command, a
         *     provider or an actor, and ``extra`` is forbidden on every model in this file, so a
         *     request carrying one is refused at the edge rather than ignored.
         */
        ConfirmRunRequest: {
            /**
             * Preview Id
             * @description The preview being confirmed. Must be the one this run published.
             */
            preview_id: string;
            /**
             * Acknowledge
             * @description Must be true. `acknowledge` is what this build's medium-risk operations require; a plan whose target is proven to carry this connection requires the `typed` method as well, and `acknowledge_object` is how that is given.
             */
            acknowledge: boolean;
            /**
             * Acknowledge Object
             * @description Required when, and only when, the plan's confirmation method is `typed`: the display name of the object this plan changes, written out. It is not a password and it does not authorise anything — what makes a guarded execution permissible is the guard, the checkpoint and the proven relation. What typing establishes is that a person looked at *which* object this is about, and the string is stored as their statement rather than compared and discarded. Supplying one for a plan that does not require it is refused, not ignored.
             */
            acknowledge_object?: string | null;
            /**
             * Expected Preview Digest
             * @description Optional concurrency check. When given it must equal the published digest, so a confirmation cannot be recorded against a plan the caller has not seen.
             */
            expected_preview_digest?: string | null;
        };
        /**
         * ConsultedSource
         * @description What one source contributed, including the ones that had nothing to say.
         *
         *     Present for every source LocalPlane knows how to consult. "NetworkManager was asked
         *     about this device and disclaims it" is a result, and without it a settled question
         *     would be indistinguishable from an unexamined one.
         */
        ConsultedSource: {
            /** Source */
            source: string;
            /** Provider */
            provider: string;
            /**
             * Status
             * @description ok | absent | unavailable | error | never_consulted. `absent` means the provider is not installed on this host, which is a conclusion — it owns nothing here. `unavailable` means it is installed and would not answer, which is a gap.
             */
            status: string;
            /**
             * Outcome
             * @description What this source said about *this* object. Stable machine-readable code.
             */
            outcome: string;
            /**
             * Gap
             * @description True when this source left the question open. A source that answered definitively while producing no claim — 'no Docker network corresponds to this link' — is not a gap.
             */
            gap: boolean;
            /** Observed At */
            observed_at: string | null;
            /** @description How old this reading is, derived at read time. */
            freshness?: components["schemas"]["Freshness"] | null;
            /** Age Seconds */
            age_seconds?: number | null;
            /** Detail */
            detail?: {
                [key: string]: unknown;
            };
        };
        /**
         * ContainerHealth
         * @description The container's own health check, when its image declares one.
         *
         *     ``checked`` is false when the image declares no health check at all — which is not the
         *     same as a check that has not run, and the two must not be confused by a reader deciding
         *     whether ``status: null`` is a gap.
         */
        ContainerHealth: {
            /** Checked */
            checked: boolean;
            /**
             * Status
             * @description healthy | unhealthy | starting, or null.
             */
            status: string | null;
            /** Failing Streak */
            failing_streak: number | null;
        };
        /**
         * ContainerImage
         * @description What this container was made from. Reported, never resolved or pulled.
         */
        ContainerImage: {
            /**
             * Reference
             * @description The image reference the container was run from.
             */
            reference: string | null;
            /**
             * Image Id
             * @description The digest the daemon actually resolved it to.
             */
            image_id: string | null;
        };
        /**
         * ContainerLifecycleOperation
         * @description Start, stop or restart one container, through Docker's own API.
         *
         *     The body is a type and an object id, and there is nothing else it will accept. The verb
         *     is *part of the type* — one of three names in a closed vocabulary — rather than a field,
         *     so there is no parameter through which a fourth could be requested, and no signal, grace
         *     period, timeout, command or payload anywhere on the path to the daemon.
         *
         *     ``extra`` is forbidden on every model here, so a request carrying one is refused at the
         *     edge rather than ignored.
         */
        ContainerLifecycleOperation: {
            /**
             * @description The typed operation. The vocabulary is closed; nothing else is accepted. (enum property replaced by openapi-typescript)
             * @enum {string}
             */
            type: "docker.container.restart" | "docker.container.start" | "docker.container.stop";
            /**
             * Object Id
             * @description The container to plan against.
             */
            object_id: string;
        };
        /** ContainerLogLine */
        ContainerLogLine: {
            /**
             * Timestamp
             * @description null when the line carried no parsable instant.
             */
            timestamp: string | null;
            /**
             * Stream
             * @description stdout | stderr | unknown
             */
            stream: string;
            /** Message */
            message: string;
        };
        /**
         * ContainerLogs
         * @description Recent output from one container. Read live, stored nowhere.
         *
         *     Bounded twice — by lines and by bytes — and neither bound is negotiable from the
         *     request beyond asking for fewer lines. There is no follow, no stream and no websocket:
         *     Docker keeps the logs and LocalPlane does not build a second copy of them.
         */
        ContainerLogs: {
            /** Object Id */
            object_id: string;
            /** Container Id */
            container_id: string;
            /**
             * Read At
             * Format: date-time
             */
            read_at: string;
            /** Requested Lines */
            requested_lines: number;
            /** Line Count */
            line_count: number;
            /**
             * Truncated
             * @description True when the byte ceiling cut the read short before the line count did.
             */
            truncated: boolean;
            /** Line Limit */
            line_limit: number;
            /** Byte Limit */
            byte_limit: number;
            /** Source */
            source: string;
            /** Lines */
            lines: components["schemas"]["ContainerLogLine"][];
        };
        /**
         * ContainerMount
         * @description Where this container keeps state. Named volumes and bind mounts alike.
         */
        ContainerMount: {
            /**
             * Type
             * @description volume | bind | tmpfs, as Docker reports it.
             */
            type: string | null;
            /**
             * Name
             * @description The volume name, for a named volume.
             */
            name: string | null;
            /**
             * Source
             * @description The host path or the volume's data directory.
             */
            source: string | null;
            /** Destination */
            destination: string | null;
            /** Driver */
            driver: string | null;
            /** Mode */
            mode: string | null;
            /** Read Write */
            read_write: boolean | null;
            /** Propagation */
            propagation: string | null;
        };
        /**
         * ContainerNetwork
         * @description One Docker network this container is attached to, as the daemon reports it.
         *
         *     A relationship Docker states, not one LocalPlane inferred. A container in the host's
         *     network namespace has no entry here at all and says so through ``network_mode``.
         */
        ContainerNetwork: {
            /** Name */
            name: string;
            /** Network Id */
            network_id: string | null;
            /** Ip Address */
            ip_address: string | null;
            /** Ipv6 Address */
            ipv6_address: string | null;
            /** Gateway */
            gateway: string | null;
            /** Mac Address */
            mac_address: string | null;
            /** Aliases */
            aliases: string[];
        };
        /**
         * ContainerObservationResult
         * @description What one container observation refresh did.
         */
        ContainerObservationResult: {
            /** Host Id */
            host_id: string;
            /** Sweep Id */
            sweep_id: string;
            /**
             * Status
             * @description ok | partial
             */
            status: string;
            /** Container Count */
            container_count: number;
            /** Issues */
            issues: components["schemas"]["ProviderIssue"][];
            /** Provider Version */
            provider_version: string;
        };
        /**
         * ContainerPort
         * @description One port the container exposes, and the host binding for it if there is one.
         */
        ContainerPort: {
            /** Container Port */
            container_port: number | null;
            /** Protocol */
            protocol: string;
            /**
             * Host Ip
             * @description null when the port is exposed but not published.
             */
            host_ip: string | null;
            /** Host Port */
            host_port: number | null;
            /** Published */
            published: boolean;
        };
        /** ContainerRestartPolicy */
        ContainerRestartPolicy: {
            /**
             * Name
             * @description no | always | unless-stopped | on-failure
             */
            name: string | null;
            /** Maximum Retry Count */
            maximum_retry_count: number | null;
        };
        /**
         * ContainerRuntime
         * @description What the container is doing now, in Docker's own words.
         */
        ContainerRuntime: {
            /**
             * State
             * @description created | running | paused | restarting | removing | exited | dead
             */
            state: string | null;
            /** Running */
            running: boolean | null;
            /** Paused */
            paused: boolean | null;
            /** Restarting */
            restarting: boolean | null;
            /** Exit Code */
            exit_code: number | null;
            /** Error */
            error: string | null;
            /** Oom Killed */
            oom_killed: boolean | null;
            /** Pid */
            pid: number | null;
            /**
             * Started At
             * @description When Docker records this container as having last started. The evidence a restart is proven from: a container running now that was running before has not been shown to have restarted.
             */
            started_at: string | null;
            /** Finished At */
            finished_at: string | null;
            /**
             * Restart Count
             * @description Restarts the daemon performed under the restart policy. Not operator ones.
             */
            restart_count: number | null;
        };
        /**
         * ContainerStats
         * @description One live sample of what a container is using. A snapshot, never a series.
         *
         *     Docker's own numbers, normalised. There is no history here and none is stored: a
         *     metrics database is a product, not a field on a container, and this build does not have
         *     one. ``gaps`` names anything the sample could not answer rather than reporting a zero
         *     somebody would act on.
         */
        ContainerStats: {
            /** Object Id */
            object_id: string;
            /** Container Id */
            container_id: string;
            /**
             * Read At
             * Format: date-time
             */
            read_at: string;
            /**
             * Sampled At
             * @description When the daemon took the sample.
             */
            sampled_at: string | null;
            /**
             * Cpu Percent
             * @description Docker's own formula. Null rather than zero when the sample had no previous reading to subtract, because zero is a number an operator would act on.
             */
            cpu_percent: number | null;
            /** Online Cpus */
            online_cpus: number | null;
            /**
             * Memory Usage Bytes
             * @description Usage with reclaimable page cache subtracted, as `docker stats` reports.
             */
            memory_usage_bytes: number | null;
            /** Memory Usage Raw Bytes */
            memory_usage_raw_bytes: number | null;
            /** Memory Limit Bytes */
            memory_limit_bytes: number | null;
            /** Memory Percent */
            memory_percent: number | null;
            /** Network Rx Bytes */
            network_rx_bytes: number | null;
            /** Network Tx Bytes */
            network_tx_bytes: number | null;
            /** Block Read Bytes */
            block_read_bytes: number | null;
            /** Block Write Bytes */
            block_write_bytes: number | null;
            /** Pids */
            pids: number | null;
            /** Pids Limit */
            pids_limit: number | null;
            /** Gaps */
            gaps: string[];
        };
        /**
         * CreateRunRequest
         * @description Plan one typed operation.
         *
         *     ``operation`` is a union discriminated on ``type`` — the shape it was designed as when
         *     it had one member. A request naming anything outside the seven closed operations is
         *     refused at the edge,
         *     before a planner is reached, by the same closed vocabulary the store CHECKs.
         */
        CreateRunRequest: {
            /** Operation */
            operation: components["schemas"]["ReconcileMtuOperation"] | components["schemas"]["ContainerLifecycleOperation"] | components["schemas"]["SystemdServiceLifecycleOperation"];
        };
        /** DatabaseStatus */
        DatabaseStatus: {
            /** Path */
            path: string;
            /**
             * Schema Versions
             * @description Migration versions applied to this store.
             */
            schema_versions: number[];
        };
        /**
         * DockerContainer
         * @description One container, as a LocalPlane resource.
         *
         *     The same shape every observed object has — identity, management, ownership, health,
         *     observation — with the Docker-specific parts kept as Docker's rather than flattened into
         *     the network-interface model. Docker remains authoritative for all of it: nothing here is
         *     a copy LocalPlane maintains, it is the newest observation of what the daemon said.
         */
        DockerContainer: {
            /** Object Id */
            object_id: string;
            /** Kind */
            kind: string;
            /** Name */
            name: string;
            /**
             * Container Id
             * @description Docker's own id. LocalPlane's identity for it.
             */
            container_id: string;
            /** Short Id */
            short_id: string;
            identity: components["schemas"]["Identity"];
            management: components["schemas"]["Management"];
            /** @description Docker created this container and Docker configures it, on the daemon's own evidence. That refuses adoption and does *not* block the lifecycle operations, because those execute through Docker's own API rather than behind its back. */
            ownership: components["schemas"]["Ownership"];
            /** @description null when the object has never been observed. */
            health: components["schemas"]["Health"] | null;
            observation: components["schemas"]["Observation"] | null;
            /**
             * Observed In Latest Sweep
             * @description Membership in the latest Docker inventory sweep.
             */
            observed_in_latest_sweep: boolean | null;
            image: components["schemas"]["ContainerImage"];
            /** Created At */
            created_at: string | null;
            runtime: components["schemas"]["ContainerRuntime"];
            container_health: components["schemas"]["ContainerHealth"];
            restart_policy: components["schemas"]["ContainerRestartPolicy"];
            /** Network Mode */
            network_mode: string | null;
            /** Networks */
            networks: components["schemas"]["ContainerNetwork"][];
            /** Ports */
            ports: components["schemas"]["ContainerPort"][];
            /** Mounts */
            mounts: components["schemas"]["ContainerMount"][];
            /**
             * Labels
             * @description The labels worth operating on: compose project and service, OCI image metadata, io.localplane.*, maintainer and description. The rest are counted and dropped — a label set is unbounded and occasionally carries secrets.
             */
            labels: {
                [key: string]: string;
            };
            /**
             * Labels Dropped
             * @description How many labels were not kept.
             */
            labels_dropped: number;
            /** Log Driver */
            log_driver: string | null;
            /** Platform */
            platform: string | null;
            /**
             * First Seen At
             * Format: date-time
             */
            first_seen_at: string;
            /**
             * Last Seen At
             * Format: date-time
             */
            last_seen_at: string;
        };
        /** DockerContainerList */
        DockerContainerList: {
            /** Host Id */
            host_id: string;
            /** @description The sweep these containers were last re-read by. An empty list with a failed sweep means nobody could look; an empty list with no sweep means nobody has. */
            last_sweep: components["schemas"]["Sweep"] | null;
            /** Count */
            count: number;
            /** Containers */
            containers: components["schemas"]["DockerContainer"][];
        };
        /** ErrorBody */
        ErrorBody: {
            /**
             * Code
             * @description Stable machine-readable code. Branch on this, not the text.
             */
            code: string;
            /** Message */
            message: string;
            /** Detail */
            detail?: {
                [key: string]: unknown;
            };
        };
        /**
         * Evidence
         * @description The raw material an observation was derived from.
         */
        Evidence: {
            /** Object Id */
            object_id: string;
            /** Observation Id */
            observation_id: string;
            /**
             * Observed At
             * Format: date-time
             */
            observed_at: string;
            /** Evidence */
            evidence: {
                [key: string]: unknown;
            };
        };
        /**
         * ExplicitIntentRevisionRequest
         * @description New desired values, supplied by the operator.
         */
        ExplicitIntentRevisionRequest: {
            /**
             * Expected Intent Id
             * @description The intent_id the caller read. Refused with 409 if it is not in force.
             */
            expected_intent_id: string;
            /**
             * Expected Version
             * @description Optional cross-check on the same version. Refused with the same 409 when it disagrees, so a client holding a stale version number is not left to find out by comparing ids.
             */
            expected_version?: number | null;
            /**
             * Fields
             * @description The controlled fields to give a new desired value, by name — currently `admin_up` (boolean) and `mtu` (integer). At least one is required. A name LocalPlane does not control is refused, never ignored, and a value of the wrong type is refused rather than coerced: `true` and `1` are different answers. Fields left out keep the value they had.
             */
            fields: {
                [key: string]: boolean | number;
            };
        };
        /**
         * FieldComparison
         * @description One typed, field-scoped verdict with the evidence behind it.
         */
        FieldComparison: {
            /** Field */
            field: string;
            /** Value Type */
            value_type: string;
            /** Intended */
            intended: boolean | number;
            /**
             * Observed
             * @description null when the value could not be read. That is unknown, never drift.
             */
            observed: boolean | number | null;
            /**
             * Comparison
             * @description matches | differs | unknown
             */
            comparison: string;
            /**
             * Reason
             * @description Stable machine-readable code. Branch on this.
             */
            reason: string;
        };
        /**
         * FieldRevision
         * @description One controlled value moving from what was intended to what is now intended.
         */
        FieldRevision: {
            /** Field */
            field: string;
            /** Value Type */
            value_type: string;
            /** Was */
            was: boolean | number;
            /** Now */
            now: boolean | number;
        };
        /**
         * Finding
         * @description A claim LocalPlane is making, with the evidence for it and its lifecycle.
         *
         *     A finding is not a state. ``reconciliation = drifted`` is recomputed from scratch every
         *     time it is asked for; this is the durable record that LocalPlane noticed, when it first
         *     noticed, and how it ended. There is no severity: ranking one disagreement against
         *     another needs a model of what the object is for, and LocalPlane does not have one.
         */
        Finding: {
            /**
             * Finding Id
             * @description This episode. A recurrence is a new one.
             */
            finding_id: string;
            /**
             * Finding Key
             * @description The stable logical identity. The same disagreement always maps here.
             */
            finding_key: string;
            /** Host Id */
            host_id: string;
            /** Object Id */
            object_id: string;
            /** Object Name */
            object_name: string;
            /** Finding Type */
            finding_type: string;
            /**
             * Subject
             * @description What within the object this is about — the field.
             */
            subject: string;
            /**
             * Status
             * @description open | resolved
             */
            status: string;
            /**
             * Summary
             * @description Derived from the typed evidence at read time, never stored.
             */
            summary: string;
            /**
             * Evidence
             * @description Shaped by `finding_type`. Both carry the typed values the claim rests on.
             */
            evidence: components["schemas"]["FindingEvidence"] | components["schemas"]["OwnershipFindingEvidence"];
            /**
             * First Seen At
             * Format: date-time
             * @description When this episode opened.
             */
            first_seen_at: string;
            /**
             * Last Seen At
             * Format: date-time
             * @description The last observation that *proved* the claim. Older than updated_at means the finding is open and the most recent look could not confirm it.
             */
            last_seen_at: string;
            /**
             * Updated At
             * Format: date-time
             * @description The last evaluation that touched this record.
             */
            updated_at: string;
            /** Resolved At */
            resolved_at: string | null;
            /**
             * Resolution
             * @description observed_matches_intent | intent_released | owner_no_longer_claims. Never a deletion.
             */
            resolution: string | null;
            /**
             * Resolved By Observation Id
             * @description For drift: the observation that proved the claim ended.
             */
            resolved_by_observation_id?: string | null;
            /**
             * Resolved By Provider Observation Id
             * @description For an ownership conflict: the provider reading that stopped claiming the object. A provider that could not be read never resolves anything.
             */
            resolved_by_provider_observation_id?: string | null;
        };
        /**
         * FindingEvidence
         * @description The typed comparison a drift claim rests on.
         */
        FindingEvidence: {
            /** Intent Id */
            intent_id: string;
            /** Field */
            field: string;
            intended: components["schemas"]["TypedValue"];
            /** @description null when the last evaluation could not read the value. */
            observed: components["schemas"]["TypedValue"] | null;
            /**
             * Comparison
             * @description differs | unknown
             */
            comparison: string;
            /** Reason */
            reason: string;
            /**
             * Observation
             * @description The observation this evidence came from.
             */
            observation: string | null;
            /** Sweep */
            sweep: string | null;
        };
        /** FindingList */
        FindingList: {
            /** Host Id */
            host_id: string;
            /**
             * Status
             * @description The filter this list was produced with.
             */
            status: string;
            /** Count */
            count: number;
            /** Findings */
            findings: components["schemas"]["Finding"][];
        };
        /**
         * Freshness
         * @description How recently LocalPlane looked at the object.
         * @enum {string}
         */
        Freshness: "current" | "stale" | "never_observed";
        /** HTTPValidationError */
        HTTPValidationError: {
            /** Detail */
            detail?: components["schemas"]["ValidationError"][];
        };
        /** Health */
        Health: {
            state: components["schemas"]["HealthState"];
            /** Reason */
            reason: string;
        };
        /**
         * HealthState
         * @description How the object is doing, independently of who manages it.
         * @enum {string}
         */
        HealthState: "healthy" | "degraded" | "failed" | "inactive" | "unknown";
        /** Host */
        Host: {
            /** Host Id */
            host_id: string;
            /**
             * Identity Basis
             * @description The evidence host_id was derived from.
             */
            identity_basis: string;
            /** Identity Confidence */
            identity_confidence: string;
            /** Hostname */
            hostname: string | null;
            /**
             * Configured Hostname
             * @description /etc/hostname. May differ from the running hostname; both are reported.
             */
            configured_hostname: string | null;
            /** Boot Id */
            boot_id: string | null;
            /** Os Id */
            os_id: string | null;
            /** Os Version Id */
            os_version_id: string | null;
            /** Os Pretty Name */
            os_pretty_name: string | null;
            /** Kernel Name */
            kernel_name: string | null;
            /** Kernel Release */
            kernel_release: string | null;
            /** Architecture */
            architecture: string | null;
            /**
             * Identity Gaps
             * @description Evidence sources that were not readable on this host.
             */
            identity_gaps: string[];
            /**
             * First Seen At
             * Format: date-time
             */
            first_seen_at: string;
            /**
             * Last Seen At
             * Format: date-time
             */
            last_seen_at: string;
            freshness: components["schemas"]["Freshness"];
            /** Age Seconds */
            age_seconds: number | null;
        };
        /** Identity */
        Identity: {
            /** Basis */
            basis: string;
            /** Value */
            value: string;
            /** Confidence */
            confidence: string;
        };
        /**
         * Intent
         * @description One version of what LocalPlane intends for an object.
         *
         *     Immutable. Adopting again, or revising, writes a new version and leaves this one in
         *     place, so what was intended, when, and on what evidence stays answerable after the fact.
         */
        Intent: {
            /** Intent Id */
            intent_id: string;
            /** Object Id */
            object_id: string;
            /** Host Id */
            host_id: string;
            /** Version */
            version: number;
            /**
             * Supersedes
             * @description The version this replaced, if any.
             */
            supersedes: string | null;
            /**
             * Schema Version
             * @description The shape of the controlled field set this intent was written against.
             */
            schema_version: number;
            /**
             * Origin
             * @description How this version came to exist: adopt | revise | adopt_runtime. Derived from the event that produced it, never from a column that could disagree with one.
             */
            origin: string;
            /** @description The revision that produced this version. null when it was adopted. */
            revision?: components["schemas"]["IntentRevision"] | null;
            /**
             * Active
             * @description Whether this version is the one currently in force for the object.
             */
            active: boolean;
            /** @description The observation this version was written against. For adopt and adopt_runtime its verified values became the intent; for an explicit revision it is the reading the operator's decision was made against, and the contract this intent is comparable under. */
            captured_from: components["schemas"]["ObservationRef"];
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /**
             * Controlled Fields
             * @description Everything LocalPlane is answerable for on this object, and nothing else. Transient link state, counters and dynamically acquired addresses are deliberately absent: they were never intended by anybody.
             */
            controlled_fields: components["schemas"]["IntentField"][];
        };
        /**
         * IntentField
         * @description One field LocalPlane controls, and the value it intends for it.
         */
        IntentField: {
            /** Field */
            field: string;
            /** Value Type */
            value_type: string;
            /** Value */
            value: boolean | number;
        };
        /** IntentHistory */
        IntentHistory: {
            /** Object Id */
            object_id: string;
            /** Active Intent Id */
            active_intent_id: string | null;
            /** Count */
            count: number;
            /**
             * Intents
             * @description Every version ever retained, newest first, each carrying the event that produced it. Reading `version`, `supersedes` and `origin` down the list reconstructs the whole chain: what was first adopted, every revision, which version superseded which, and which one is in force now.
             */
            intents: components["schemas"]["Intent"][];
            /**
             * Transitions
             * @description Adopt and release, newest first. Release leaves no other trace — the intent it retired is unchanged — so this is where the moment it stopped applying is. Revisions are not here: they do not move the management state, and a managed → managed row would describe a movement that did not happen. They are on the versions they produced.
             */
            transitions: components["schemas"]["ManagementTransition"][];
        };
        /**
         * IntentRevision
         * @description The event that produced one intent version by revising the one before it.
         */
        IntentRevision: {
            /** Revision Id */
            revision_id: string;
            /**
             * Kind
             * @description revise — the operator supplied a new desired value. adopt_runtime — the operator declared that what was observed is what was wanted. Different acts, kept apart so a history can still tell them apart.
             */
            kind: string;
            /**
             * Host Effect
             * @description Always 'none'. The store cannot record otherwise.
             */
            host_effect: string;
            /**
             * Occurred At
             * Format: date-time
             */
            occurred_at: string;
        };
        /**
         * IntentRevisionRequest
         * @description The common part of both revisions: which version this decision was made against.
         *
         *     ``expected_intent_id`` is required, and it is the whole of the lost-update defence. Two
         *     operators looking at the same drift will both read the intent in force, and whichever
         *     writes second must not silently overwrite a decision they never saw. A caller that
         *     names a version which is no longer active is told so and can decide again against the
         *     current desired state, which is the only safe thing to do with somebody else's
         *     judgement.
         */
        IntentRevisionRequest: {
            /**
             * Expected Intent Id
             * @description The intent_id the caller read. Refused with 409 if it is not in force.
             */
            expected_intent_id: string;
            /**
             * Expected Version
             * @description Optional cross-check on the same version. Refused with the same 409 when it disagrees, so a client holding a stale version number is not left to find out by comparing ids.
             */
            expected_version?: number | null;
        };
        /**
         * IntentRevisionResult
         * @description The result of revising a managed object's retained intent.
         *
         *     ``host_mutated`` is ``false`` and there is no code path on which it could be anything
         *     else. A revision replaces one record of what LocalPlane wants with another; no link was
         *     brought up or down, no MTU was set, and this revision path reaches none of the agent's
         *     mutating methods — those belong to the apply of a Run. If the runtime and the intent
         *     now agree, they agree because the intent moved.
         */
        IntentRevisionResult: {
            /**
             * Kind
             * @description revise | adopt_runtime — the same value the new intent carries as `origin`, stated here because it is what this response is about.
             */
            kind: string;
            /** Object Id */
            object_id: string;
            /** Host Id */
            host_id: string;
            /** @description Unchanged, and reported so a caller can see it is unchanged. Revision is not a transition: the object was managed before and is managed after. */
            management: components["schemas"]["Management"];
            /**
             * Host Mutated
             * @description Always false. This revision changed LocalPlane's records only.
             */
            host_mutated: boolean;
            /**
             * Host Effect
             * @description Always 'none'.
             */
            host_effect: string;
            /** Note */
            note: string;
            /** Revision Id */
            revision_id: string;
            /**
             * Occurred At
             * Format: date-time
             */
            occurred_at: string;
            /** @description The version that was in force, exactly as it still is. Nothing about it was rewritten; it stopped being active, which is a different thing. */
            previous_intent: components["schemas"]["Intent"];
            /** @description The version now in force. */
            intent: components["schemas"]["Intent"];
            /**
             * Changed Fields
             * @description The controlled values whose intended value this revision moved.
             */
            changed_fields: components["schemas"]["FieldRevision"][];
            /**
             * Carried Forward
             * @description Controlled fields the operator did not name, kept at their existing intended value. Empty for adopt_runtime, where every value was read from the observation — including the ones that already agreed.
             */
            carried_forward: string[];
            /** @description Recomputed against the new intent and the newest observation. It may well be in_sync, and if it is, nothing about the host produced that. */
            reconciliation: components["schemas"]["Reconciliation"];
            /**
             * Findings Resolved
             * @description Drift findings closed by this revision, by id. Their resolution is `intent_revised`, never `observed_matches_intent`: nothing was remediated.
             */
            findings_resolved?: string[];
            /**
             * Findings Opened
             * @description Drift findings this revision opened — a new desired value the runtime does not have is a new disagreement, and it is recorded as one.
             */
            findings_opened?: string[];
            /** @description What LocalPlane knew about who owns this object when it agreed to revise, including any source it could not consult. */
            ownership?: components["schemas"]["Ownership"] | null;
        };
        /**
         * IntentSummary
         * @description The active intent, reduced to what a list view needs.
         */
        IntentSummary: {
            /** Intent Id */
            intent_id: string;
            /** Version */
            version: number;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /** Controlled Fields */
            controlled_fields: string[];
        };
        /**
         * KeepGuardedRunRequest
         * @description Keep a guarded change. One field, and it names nothing.
         *
         *     There is deliberately no object id, no value, no interface, no deadline and no guard id
         *     here: what authorises keeping a guarded change is not something a caller can send, it is
         *     the management path this request itself re-proves over the object that was changed. A
         *     body that could name anything would be a body that could name the wrong thing.
         */
        KeepGuardedRunRequest: {
            /**
             * Acknowledge
             * @description Must be true. Keeping a change to the object carrying your own connection is a deliberate act, and a request that did not say so would be one a stray client could make.
             */
            acknowledge: boolean;
        };
        /** Link */
        Link: {
            /** Ifindex */
            ifindex: number | null;
            /** Mtu */
            mtu: number | null;
            /** Mac Address */
            mac_address: string | null;
            /** Mac Is Permanent */
            mac_is_permanent: boolean | null;
            /** Admin Up */
            admin_up: boolean | null;
            /** Operstate */
            operstate: string | null;
            /**
             * Carrier
             * @description null while the link is administratively down — the kernel refuses the read.
             */
            carrier: boolean | null;
            /**
             * Speed Mbps
             * @description null when the kernel does not know it.
             */
            speed_mbps: number | null;
            /** Duplex */
            duplex: string | null;
            /**
             * Link Kind
             * @description IFLA_INFO_KIND, e.g. bridge, veth, tun.
             */
            link_kind: string | null;
            /** Arphrd Type */
            arphrd_type: number | null;
            /** Is Physical */
            is_physical: boolean | null;
            /** Device Path */
            device_path: string | null;
            /** Master */
            master: string | null;
            /** Carrier Changes */
            carrier_changes: number | null;
        };
        /** Management */
        Management: {
            state: components["schemas"]["ManagementState"];
            /** Reason */
            reason: string;
        };
        /**
         * ManagementPath
         * @description Which object carries the connection this request arrived on, or why that is unknown.
         *
         *     The answer depends on the connection asking, and deliberately so. Evidence proving that
         *     one operator's session terminates on one object says nothing about a second operator's
         *     session, about automation calling over loopback, or about the same operator arriving on
         *     a different address — and a judgement that an object is safe to change rests on evidence
         *     about the session it would be changed from.
         *
         *     Confirming it requires two independent sources to agree: the local address this
         *     connection terminated on must be present on exactly one currently observed object, and
         *     the kernel's route to the peer must leave by that same object. Disagreement resolves
         *     nothing and is reported as a conflict.
         */
        ManagementPath: {
            /** Host Id */
            host_id: string;
            /**
             * State
             * @description confirmed | unresolved
             */
            state: string;
            /**
             * Object Id
             * @description The object carrying the path. Null unless state is confirmed.
             */
            object_id: string | null;
            /** Object Name */
            object_name: string | null;
            /**
             * Reason
             * @description The typed code for this answer — `management_path_confirmed`, or which piece of evidence was missing, unusable or in conflict.
             */
            reason: string;
            /**
             * Missing Evidence
             * @description What would settle it, named rather than worked around.
             */
            missing_evidence: string[];
            transport: components["schemas"]["TransportEvidenceView"];
            /** @description The observation this answer was derived from, when there is one. */
            evidence: components["schemas"]["ManagementPathEvidence"] | null;
            /**
             * Evidence Ttl Seconds
             * @description How long an observation vouches for a path. The same horizon every other observation in LocalPlane uses: an address can be removed and a route replaced at any time, so this evidence is not entitled to a longer life than the readings the rest of the product rests on.
             */
            evidence_ttl_seconds: number;
            /**
             * As Of
             * Format: date-time
             */
            as_of: string;
        };
        /**
         * ManagementPathEvidence
         * @description One recorded observation of how a connection reached LocalPlane.
         *
         *     Raw evidence, kept so that a protection judgement stays answerable afterwards. It does
         *     not hold a conclusion: which object carries the path depends on facts that move
         *     independently of this record, so the verdict is derived at read time and this is what
         *     made it believable.
         */
        ManagementPathEvidence: {
            /** Observation Id */
            observation_id: string;
            /** Host Id */
            host_id: string;
            /**
             * Observed At
             * Format: date-time
             */
            observed_at: string;
            /** Agent Instance Id */
            agent_instance_id: string | null;
            /** Capability */
            capability: string;
            /** Provider */
            provider: string;
            /** Provider Version */
            provider_version: string;
            /** Method */
            method: string;
            /** Transport Peer Address */
            transport_peer_address: string;
            /** Local Endpoint Address */
            local_endpoint_address: string;
            /** Family */
            family: string;
            route: components["schemas"]["RouteEvidenceView"];
        };
        /**
         * ManagementPathObservationResult
         * @description The outcome of observing the management path for this request.
         *
         *     Read-only with respect to the host. The kernel is asked which route it would use to
         *     reach the peer — an `RTM_GETROUTE` netlink query, a question — and nothing is created,
         *     modified, brought up or brought down.
         */
        ManagementPathObservationResult: {
            /** Host Id */
            host_id: string;
            /**
             * Host Mutated
             * @description Always false. This observes; it does not change.
             */
            host_mutated: boolean;
            /**
             * Host Effect
             * @description Always 'none'.
             */
            host_effect: string;
            /**
             * Recorded
             * @description Whether evidence was written. False when the transport could not establish anything — a request from loopback leaves no row, because a record that looks like evidence and proves nothing is worse than no record.
             */
            recorded: boolean;
            /** Note */
            note: string;
            management_path: components["schemas"]["ManagementPath"];
        };
        /**
         * ManagementState
         * @description LocalPlane's stance towards an object.
         * @enum {string}
         */
        ManagementState: "observe_only" | "observed" | "managed";
        /**
         * ManagementTransition
         * @description An adopt or a release. Neither writes to the host.
         */
        ManagementTransition: {
            /** Transition Id */
            transition_id: string;
            /**
             * Transition
             * @description adopt | release
             */
            transition: string;
            from_state: components["schemas"]["ManagementState"];
            to_state: components["schemas"]["ManagementState"];
            /** Intent Id */
            intent_id: string;
            /**
             * Observation Id
             * @description The evidence adopt captured. Release consults none.
             */
            observation_id: string | null;
            /**
             * Host Effect
             * @description Always 'none'. The store cannot record a host write on this path.
             */
            host_effect: string;
            /**
             * Occurred At
             * Format: date-time
             */
            occurred_at: string;
        };
        /** NetworkInterface */
        NetworkInterface: {
            /** Object Id */
            object_id: string;
            /** Kind */
            kind: string;
            /** Name */
            name: string;
            /**
             * Interface Kind
             * @description What the kernel says this is: ethernet, wireless, bridge, tunnel, …
             */
            interface_kind: string;
            identity: components["schemas"]["Identity"];
            management: components["schemas"]["Management"];
            /** @description null unless management.state is 'managed'. An observed object has no retained intent, so it cannot drift — which is not the same as being in sync. */
            reconciliation?: components["schemas"]["ReconciliationState"] | null;
            /** @description The intent in force, when there is one. Its controlled_fields are the whole of what LocalPlane is answerable for on this object; everything else in this response is observation, not intention. */
            intent?: components["schemas"]["IntentSummary"] | null;
            /** @description Who made this object and who configures it, on the evidence. A separate axis from management: an object another system owns is still `observed`, and what changes is that `ownership.adoption` refuses it. */
            ownership: components["schemas"]["Ownership"];
            /** @description null when the object has never been observed. */
            health: components["schemas"]["Health"] | null;
            observation: components["schemas"]["Observation"] | null;
            /**
             * Observed In Latest Sweep
             * @description Membership in the latest inventory sweep for this capability. A newer targeted observation can update this object's facts without replacing that estate-completeness boundary.
             */
            observed_in_latest_sweep: boolean | null;
            link: components["schemas"]["Link"] | null;
            /**
             * Addresses
             * @description [] means no addresses. null means no address source was available.
             */
            addresses: components["schemas"]["Address"][] | null;
            statistics: components["schemas"]["Statistics"] | null;
            /**
             * First Seen At
             * Format: date-time
             */
            first_seen_at: string;
            /**
             * Last Seen At
             * Format: date-time
             */
            last_seen_at: string;
        };
        /** NetworkInterfaceList */
        NetworkInterfaceList: {
            /** Host Id */
            host_id: string;
            /** @description The sweep these objects were last re-read by. An empty list with a failed sweep means nobody could look; an empty list with no sweep means nobody has. */
            last_sweep: components["schemas"]["Sweep"] | null;
            /** Count */
            count: number;
            /** Interfaces */
            interfaces: components["schemas"]["NetworkInterface"][];
        };
        /**
         * ObjectProtection
         * @description Whether this object is protected, why, and on what evidence.
         *
         *     A separate axis from ownership, and the two answer different questions. Ownership asks
         *     whose the object is; protection asks what changing it would put at risk. An object can
         *     be LocalPlane's own and protected, or externally configured and not protected at all,
         *     and a Run can truthfully publish `protection: clear` while remaining blocked because
         *     another system demonstrably configures the object.
         *
         *     `clear` is scoped to the reasons assessed for this object and operation. It is not a
         *     word for `safe`.
         */
        ObjectProtection: {
            /** Object Id */
            object_id: string;
            /** Object Name */
            object_name: string;
            /**
             * Status
             * @description protected | clear | unknown
             */
            status: string;
            /**
             * Reasons
             * @description Reasons proven to apply.
             */
            reasons: string[];
            /**
             * Unresolved
             * @description Reasons whose evidence could not be settled.
             */
            unresolved: string[];
            /**
             * Management Path
             * @description on_management_path | not_on_management_path | unknown
             */
            management_path: string;
            /** Reason */
            reason: string;
            /** Missing Evidence */
            missing_evidence: string[];
            /** Assessed */
            assessed: components["schemas"]["ProtectionReasonView"][];
            /**
             * Implemented Reasons
             * @description Every protection reason evaluated for this object. `clear` means these were evaluated and none applied — which is why the list is published rather than left for a reader to assume.
             */
            implemented_reasons: string[];
            /** Note */
            note: string;
            /**
             * As Of
             * Format: date-time
             */
            as_of: string;
        };
        /** Observation */
        Observation: {
            /** Observation Id */
            observation_id: string;
            /** Sweep Id */
            sweep_id: string;
            /**
             * Observed At
             * Format: date-time
             */
            observed_at: string;
            /**
             * Received At
             * Format: date-time
             */
            received_at: string;
            freshness: components["schemas"]["Freshness"];
            /** Age Seconds */
            age_seconds: number | null;
            /** Provider */
            provider: string;
            /** Provider Version */
            provider_version: string;
            /** Method */
            method: string;
            /** Capability */
            capability: string;
            /**
             * Fidelity
             * @description complete | partial | degraded — how whole the evidence is.
             */
            fidelity: string;
            /**
             * Gaps
             * @description Fields the source did not supply a value for. They are null, not zero.
             */
            gaps: string[];
        };
        /**
         * ObservationRef
         * @description Which reading a record was derived from, and what produced it.
         */
        ObservationRef: {
            /** Observation Id */
            observation_id: string;
            /** Sweep Id */
            sweep_id: string;
            /**
             * Observed At
             * Format: date-time
             */
            observed_at: string;
            /** Capability */
            capability: string;
            /** Provider */
            provider: string;
            /** Provider Version */
            provider_version: string;
        };
        /**
         * Owner
         * @description A system, and the specific thing within it that the claim is about.
         */
        Owner: {
            /**
             * Provider
             * @description docker | networkmanager | tailscale | kernel
             */
            provider: string;
            /**
             * Instance
             * @description The identifier inside that system — a Docker network id, a NetworkManager connection uuid, a device path. What makes the claim checkable against the provider itself.
             */
            instance: string | null;
            /**
             * Label
             * @description That thing's human name, when it has one.
             */
            label: string | null;
            /**
             * Version
             * @description The provider's version, when it reports one.
             */
            version: string | null;
        };
        /**
         * Ownership
         * @description What LocalPlane knows about who owns this object, and what follows from it.
         *
         *     ``state`` is ``attributed`` when there is at least one evidence-backed claim and
         *     ``unknown`` otherwise — and ``unknown`` is a real answer with two distinct causes that
         *     ``reason`` separates: every source was consulted and none claimed the object
         *     (``no_provider_claim``), or something could not be consulted (``evidence_incomplete``).
         *
         *     Nothing here is a management state. An object Docker owns is still ``observed``; what
         *     ownership changes is ``adoption``.
         */
        Ownership: {
            /**
             * State
             * @description attributed | unknown
             */
            state: string;
            /**
             * Reason
             * @description externally_configured | externally_created | conflicting_claims | host_kernel_only | evidence_incomplete | no_provider_claim | no_observation
             */
            reason: string;
            created_by?: components["schemas"]["OwnershipClaimSummary"] | null;
            configured_by?: components["schemas"]["OwnershipClaimSummary"] | null;
            /**
             * Evidence Gaps
             * @description Sources that left the question open.
             */
            evidence_gaps?: string[];
            adoption: components["schemas"]["AdoptionEligibility"];
        };
        /** OwnershipClaim */
        OwnershipClaim: {
            /** Relation */
            relation: string;
            owner: components["schemas"]["Owner"];
            /** Confidence */
            confidence: string;
            /** Reason */
            reason: string;
            /** Evidence */
            evidence: components["schemas"]["OwnershipEvidenceItem"][];
        };
        /**
         * OwnershipClaimSummary
         * @description One relation and its owner, without the evidence payload.
         *
         *     The evidence itself is at ``/network/interfaces/{id}/provenance``; what is here is the
         *     reason code, which names the *kind* of evidence the claim rests on without carrying it.
         */
        OwnershipClaimSummary: {
            /**
             * Relation
             * @description created_by — what brought the object into existence and would recreate it. configured_by — what is applying configuration to it now. They are different questions and are frequently answered by different systems.
             */
            relation: string;
            owner: components["schemas"]["Owner"];
            /**
             * Confidence
             * @description confirmed — the provider names this object itself and the kernel agrees. corroborated — the provider's declaration matches, uniquely, a fact the kernel reports. There is no weaker value: LocalPlane makes no guesses.
             */
            confidence: string;
            /**
             * Reason
             * @description Stable machine-readable code. Branch on this.
             */
            reason: string;
            /**
             * Evidence Sources
             * @description Which sources argued for this claim. Their content is on the detail resource.
             */
            evidence_sources: string[];
        };
        /**
         * OwnershipEvidenceItem
         * @description One machine-readable fact behind a claim, with the values that were compared.
         */
        OwnershipEvidenceItem: {
            /** Source */
            source: string;
            /**
             * Kind
             * @description What kind of fact this is, e.g. declared_bridge_name, ipam_gateway_address.
             */
            kind: string;
            /**
             * Detail
             * @description Exactly what was compared, so the claim can be checked rather than trusted.
             */
            detail: {
                [key: string]: unknown;
            };
            /** Observed At */
            observed_at: string | null;
        };
        /**
         * OwnershipFindingEvidence
         * @description The claim an ownership conflict rests on.
         *
         *     A different kind of evidence from drift's, which is why it is a different shape. Drift
         *     compares one typed scalar against one controlled field; this names a relation, an owner
         *     and the source that established it. Squeezing the second into the first's fields would
         *     mean an ``intended`` value that stands for nothing.
         */
        OwnershipFindingEvidence: {
            /**
             * Intent Id
             * @description The intent this conflicts with.
             */
            intent_id: string;
            /**
             * Relation
             * @description created_by | configured_by
             */
            relation: string;
            owner: components["schemas"]["Owner"];
            /**
             * Confidence
             * @description confirmed | corroborated
             */
            confidence: string;
            /** Evidence Source */
            evidence_source: string;
            /** Reason */
            reason: string;
            /**
             * Provider Observation
             * @description The provider reading that established the claim.
             */
            provider_observation: string | null;
            /** Observation */
            observation: string | null;
            /** Sweep */
            sweep: string | null;
        };
        /** PlanAuthorization */
        PlanAuthorization: {
            /**
             * State
             * @description not_preflighted
             */
            state: string;
            /** Exact */
            exact: boolean;
            /** Authority */
            authority: string;
            /** Decision Point */
            decision_point: string;
            /** Action Id */
            action_id: string;
            /** Canonical Target */
            canonical_target: string;
            /** Verb */
            verb: string;
            /** Reason */
            reason: string;
        };
        /**
         * PlanConfirmation
         * @description What confirming this plan takes, and whether it has been confirmed.
         *
         *     ``token_issued`` is ``false`` on every path and the store will accept nothing else. A
         *     confirmation is a row naming this Run and this plan, not a bearer value handed to a
         *     caller: there is nothing here that could be presented to authorise anything else.
         *     ``satisfied`` is a fact about the Run rather than about the immutable document, and it
         *     is rendered from whether a confirmation has actually been recorded for it.
         */
        PlanConfirmation: {
            /** Required */
            required: boolean;
            /**
             * Method
             * @description none | acknowledge | typed
             */
            method: string;
            /**
             * Source
             * @description policy | operation. An operation may make the requirement stronger and never weaker, and which of the two decided is recorded.
             */
            source: string;
            /** Reasons */
            reasons: string[];
            /**
             * Policy
             * @description The policy sentence in force when this was published. Stored, so a plan reviewed under one policy is not silently re-read under a later one.
             */
            policy: string;
            /**
             * Token Issued
             * @description Always false. Nothing is issued that could be presented elsewhere.
             */
            token_issued: boolean;
            /**
             * Satisfied
             * @description Whether a confirmation has been recorded for this run.
             */
            satisfied: boolean;
            /**
             * Satisfiable
             * @description Whether execution is available at all for this operation.
             */
            satisfiable: boolean;
            /** Unsatisfiable Reason */
            unsatisfiable_reason: string | null;
        };
        /** PlanEffectEdge */
        PlanEffectEdge: {
            /** Source */
            source: string;
            /** Relation */
            relation: string;
            /** Target */
            target: string;
        };
        /**
         * PlanEvidence
         * @description Exactly which records this plan was derived from.
         *
         *     ``intent`` is null for an action, structurally: LocalPlane retains no desired state for
         *     something it only acts on, so there is no version to cite and no contract to compare
         *     under. A reader should not take the null as "the intent could not be loaded".
         */
        PlanEvidence: {
            intent?: components["schemas"]["PlanIntentRef"] | null;
            observation: components["schemas"]["PlanObservationRef"];
            ownership: components["schemas"]["PlanOwnership"];
        };
        /**
         * PlanExecution
         * @description HOW it would run, and everything that says it would not.
         *
         *     Two different questions, answered separately on purpose. ``availability`` is about this
         *     build — whether LocalPlane has any code that would execute this. ``eligibility`` is
         *     about this plan — whether it would be allowed to run if it did. A plan can be perfectly
         *     describable and completely ineligible, and saying both is more useful than refusing to
         *     describe it.
         */
        PlanExecution: {
            /**
             * Availability
             * @description available | unavailable | not_implemented. This describes whether the build has an executor for this operation; every operation this build plans has one, so `not_implemented` appears only on previews stored by an earlier build, or on an operation this build could no longer execute.
             */
            availability: string;
            /**
             * Eligibility
             * @description eligible | guarded | blocked. `guarded` means ordinary execution is blocked and the connection-guarded path is the only write path that exists for this plan — which happens for exactly one situation: a target *proven* to carry the management path this request arrived over. A target whose relation cannot be established is `blocked` and stays there.
             */
            eligibility: string;
            /**
             * Blockers
             * @description Every reason, not the first. An operator who fixes one and returns to find another they were never told about has been given the plan in instalments.
             */
            blockers: string[];
            /**
             * Provider
             * @description The provider that would perform the write, when one is truthfully known — every operation this build plans names its executor's provider. `null` means no provider truthfully owns this write, and naming a plausible command, binary or daemon would be inventing an execution path.
             */
            provider: string | null;
            /**
             * Required Capability
             * @description The agent capability an execution would need. Deliberately not in the protocol's capability list — that describes what the agent can do.
             */
            required_capability: string;
            /**
             * Capability Declared By Agent
             * @description Whether the agent probed and declared it. Checked, not assumed.
             */
            capability_declared_by_agent: boolean;
            /** Note */
            note: string;
        };
        /**
         * PlanGuard
         * @description What a connection guard would be for this plan. Nothing here is armed.
         *
         *     A connection guard is a **reversal held on the host with a deadline**, dispatched with
         *     no further request from anybody if nothing proves within the window that this console
         *     can still be reached over the object being changed. It is what makes a change to that
         *     object survivable rather than merely refused: the outage a mistake can cause is bounded
         *     by the window, and the bound is enforced by a component that does not depend on this
         *     connection, on this request, or on the process serving it.
         *
         *     ``armed`` is ``false`` on every published plan and the store will not accept anything
         *     else — the same rule ``recovery.armed`` follows, for the same reason.
         */
        PlanGuard: {
            /**
             * Availability
             * @description available | unavailable
             */
            availability: string;
            /**
             * Reason
             * @description The typed code. `guarded_execution_available`, or why not: `guard_not_required` (the target is not the management path, so the ordinary path is open and a guard would be a mechanism against no hazard), `operation_has_no_unattended_reversal`, `management_path_unproven`, `guard_capability_undeclared`.
             */
            reason: string;
            /**
             * Window S
             * @description How long the guard would hold. A policy constant: there is no request field, query parameter or setting through which a caller could choose it, because a caller who could choose it could choose one that never usefully fires.
             */
            window_s: number;
            /**
             * Prerequisites
             * @description Every condition that must be proven before a guard is armed, in the order they are evaluated. All of them are re-proved from fresh evidence at apply time; the plan states them so an operator can see what the guard rests on.
             */
            prerequisites: string[];
            /**
             * Unmet
             * @description The ones that could not be proven when this plan was published. `host_side_guard_accepted` is never among them here — it cannot be evaluated before the attempt, and a plan that claimed it would be claiming a guard.
             */
            unmet: string[];
            /**
             * Armed
             * @description Always false. A plan is published before anything is armed.
             */
            armed: boolean;
            /** Guarantee */
            guarantee: string;
        };
        /** PlanIntentRef */
        PlanIntentRef: {
            /** Intent Id */
            intent_id: string;
            /** Version */
            version: number;
            /** Capability */
            capability: string;
            /** Provider */
            provider: string;
        };
        /** PlanObservationRef */
        PlanObservationRef: {
            /** Observation Id */
            observation_id: string;
            /** Sweep Id */
            sweep_id: string;
            /**
             * Observed At
             * Format: date-time
             */
            observed_at: string;
        };
        /**
         * PlanOwnership
         * @description Ownership as it stood when the plan was published, not as it stands now.
         *
         *     A plan is a decision made against the evidence that existed when it was made. A preview
         *     whose ownership section improved overnight would be a different plan wearing the same
         *     identity — so this is stored, and a change to it makes the preview stale instead.
         */
        PlanOwnership: {
            /** State */
            state: string;
            /** Reason */
            reason: string;
            /** Claims */
            claims: components["schemas"]["PublishedOwnershipClaim"][];
            /**
             * Evidence Gaps
             * @description Sources that left the question open. Unlike adoption, these block execution: adopt records values the host already has, and a write would be LocalPlane acting.
             */
            evidence_gaps: string[];
            /**
             * Provider Readings
             * @description Which reading each source was assessed from, by provider.
             */
            provider_readings: {
                [key: string]: unknown;
            };
        };
        /**
         * PlanProtection
         * @description Whether the target is protected, why, and what the answer rested on.
         *
         *     Frozen into the preview as it stood when the plan was published, not re-derived on
         *     every read. A plan is a decision made against the evidence that existed when it was
         *     made, and a preview whose protection section improved overnight would be a different
         *     plan wearing the same identity.
         *
         *     ``unknown`` is a real answer, and it is never softened into ``clear``. The management
         *     path is proven only from the transport a request actually arrived on and the kernel's
         *     own route to its peer; it is never inferred from an interface's name, from which link
         *     carries the default route, from a proxy header, or from a claim in a request body.
         */
        PlanProtection: {
            /**
             * Status
             * @description protected | clear | unknown. `clear` means every protection reason this build implements was evaluated and none applies — it is not a word for `safe`.
             */
            status: string;
            /**
             * Reasons
             * @description Protection reasons proven to apply. Empty unless status is protected.
             */
            reasons: string[];
            /**
             * Unresolved
             * @description Reasons whose evidence could not be settled. Empty unless unknown.
             */
            unresolved: string[];
            /**
             * Management Path
             * @description on_management_path | not_on_management_path | unknown — this target's relation to the operator's path: the target, not the target, or unresolved.
             */
            management_path: string;
            /** Reason */
            reason: string;
            /**
             * Missing Evidence
             * @description The evidence that would settle it, named rather than worked around.
             */
            missing_evidence: string[];
            /**
             * Evidence Id
             * @description The management-path observation this judgement was made from. Bound to the preview, and deliberately not part of its digest: a later observation proving the same path has confirmed the plan, not changed it.
             */
            evidence_id?: string | null;
            /** Evidence Observed At */
            evidence_observed_at?: string | null;
            /**
             * Assessed
             * @description Every independently evaluated reason and its immutable evidence.
             */
            assessed?: components["schemas"]["PlanProtectionReason"][];
        };
        /** PlanProtectionReason */
        PlanProtectionReason: {
            /** Reason */
            reason: string;
            /** Status */
            status: string;
            /** Detail */
            detail: string;
            /** Evidence Id */
            evidence_id?: string | null;
            /** Observed At */
            observed_at?: string | null;
            /** Evidence */
            evidence?: {
                [key: string]: unknown;
            };
            /** Missing Evidence */
            missing_evidence?: string[];
        };
        /**
         * PlanRationale
         * @description WHY this plan exists.
         *
         *     ``intent_id`` is null for an action, and structurally so: LocalPlane retains no desired
         *     state for something it only acts on, nothing has drifted, and there is no version chain.
         *     A reader should not take the null as "the intent could not be loaded".
         */
        PlanRationale: {
            /** Intent Id */
            intent_id?: string | null;
            /** Intent Version */
            intent_version?: number | null;
            /**
             * Reason
             * @description Stable code for what motivates the plan.
             */
            reason: string;
            /**
             * Drift Finding Id
             * @description The durable claim this plan answers, when LocalPlane had one open. Evidence rather than a precondition: a disagreement is real whether or not a sweep has yet turned it into a finding.
             */
            drift_finding_id: string | null;
        };
        /**
         * PlanRecovery
         * @description What recovery would look like, and the fact that none of it is armed.
         *
         *     ``armed`` is ``false`` on every path and the store will not accept anything else. "A
         *     rollback exists in principle" and "recovery is established and will fire" are different
         *     claims, and that difference is the whole point of the write-boundary rules.
         */
        PlanRecovery: {
            /**
             * Mode
             * @description auto | operator | none
             */
            mode: string;
            /**
             * Rollback Possible
             * @description Whether a rollback exists in principle for this operation.
             */
            rollback_possible: boolean;
            /**
             * Restores Field
             * @description The field a rollback would restore, or null where there is nothing to.
             */
            restores_field: string | null;
            /**
             * Restores Value
             * @description The value that would be put back: the one the host carries now. Null for an operation with no inverse LocalPlane may perform, where an inverse is not 'the opposite verb' but a restoration nobody has to be asked about.
             */
            restores_value: boolean | number | null;
            /**
             * Armed
             * @description Always false. Nothing is holding this value.
             */
            armed: boolean;
            /**
             * Guarantee
             * @description Always 'none' while nothing is armed.
             */
            guarantee: string;
            /** Reason */
            reason: string;
        };
        /**
         * PlanRisk
         * @description RISK, derived from evidence and never declared alone.
         *
         *     An operation carries a base tier; ownership and protection can raise it and nothing
         *     lowers it. A factor that merely failed to lower the tier is still listed, because
         *     "LocalPlane could not rule this out" is the kind of evidence that otherwise disappears.
         */
        PlanRisk: {
            /**
             * Tier
             * @description low | medium | high
             */
            tier: string;
            /** Factors */
            factors: components["schemas"]["RiskFactor"][];
        };
        /**
         * PlanRuntimeOwnerCorrelation
         * @description Digest-bound ``docker-direct-unix-v1`` semantics, never transient handles.
         */
        PlanRuntimeOwnerCorrelation: {
            /** Contract Version */
            contract_version: string;
            /** Method Version */
            method_version: number;
            /** Provider */
            provider: string;
            /** Status */
            status: string;
            /** Attestation Fingerprint */
            attestation_fingerprint: string | null;
            /** Endpoint */
            endpoint: string | null;
            /** Container Id */
            container_id: string | null;
            /** Container Started At */
            container_started_at: string | null;
            /** Engine Id */
            engine_id: string | null;
            /** Direct Transport Verified */
            direct_transport_verified: boolean;
            /** Peer Service Main Verified */
            peer_service_main_verified: boolean;
            /** Owner Unit Id */
            owner_unit_id: string | null;
            /** Owner Invocation Id */
            owner_invocation_id: string | null;
            /** Execution Cgroup Relation */
            execution_cgroup_relation: string | null;
            /** Gaps */
            gaps: string[];
        };
        /**
         * PlanSelfImpact
         * @description Whether executing this plan could interrupt the LocalPlane backend publishing it.
         *
         *     Derived from the protection assessment and the lifecycle context, and it moves neither.
         *     A plan carrying this section is exactly as protected as it would be without one; what
         *     it adds is *which* hazard inside that verdict is LocalPlane itself.
         *
         *     **`override_eligible` is not permission and grants nothing.** No authority exists in
         *     this build — no grant, no endpoint, no execution path reads it. It says only whether
         *     this exact hazard is the shape a narrowly typed operator authority could later be
         *     issued against, published now so that what such an authority would name is a document
         *     you can already read.
         */
        PlanSelfImpact: {
            /**
             * Subject
             * @description localplane_backend_runtime — the only one assessed.
             */
            subject: string;
            /**
             * Status
             * @description not_detected | proven | possible | unresolved. `not_detected` is earned from evidence about where the backend runs, never from an absence of it.
             */
            status: string;
            /**
             * Outage
             * @description none | temporary_possible | indefinite_possible. A restart may interrupt LocalPlane and it may return once the runtime recovers; a stop may take it away with nothing scheduled to bring it back. Neither promises a return, and no second path to this host has been verified.
             */
            outage: string;
            /**
             * Override Eligible
             * @description Whether this hazard is the one shape a future typed authority could cover. Not permission, and nothing in this build acts on it.
             */
            override_eligible: boolean;
            /**
             * Detail
             * @description The typed code for the status. Branch on this.
             */
            detail: string;
            /**
             * Owner Unit Id
             * @description The unit whose interruption would take the backend with it.
             */
            owner_unit_id?: string | null;
            /**
             * Envelope
             * @description The closed contract the proof came under, when eligible.
             */
            envelope?: string | null;
            /**
             * Reasons
             * @description Typed codes for every reason this is not eligible. Empty when it is.
             */
            reasons?: string[];
            /**
             * Required
             * @description Whether this plan's only write path is the override — `how.eligibility` is `self_impact_override_required`. A fact about the plan, not about the document: `override_eligible` says the hazard *could* be authorised, this says it is the one thing standing in the way.
             * @default false
             */
            required: boolean;
            /**
             * Granted
             * @description Whether an operator has granted the override for this run. Never a token.
             * @default false
             */
            granted: boolean;
            /**
             * Consumed
             * @description Whether that grant has been spent. It is single-use.
             * @default false
             */
            consumed: boolean;
        };
        /** PlanSystemdLifecycleContext */
        PlanSystemdLifecycleContext: {
            /** Status */
            status: string;
            /** Target Unit */
            target_unit: string;
            /** Action */
            action: string;
            /** Effect Units */
            effect_units: string[];
            /** Effect Edges */
            effect_edges: components["schemas"]["PlanEffectEdge"][];
            /** Effect Complete */
            effect_complete: boolean;
            /** Active Activation Sources */
            active_activation_sources: string[];
            /** Active Upholding Sources */
            active_upholding_sources: string[];
            /** Management Units */
            management_units: string[];
            /** Management Complete */
            management_complete: boolean;
            /**
             * Connection Unit
             * @description The systemd unit containing the connection this request arrived over — the one hosting LocalPlane's backend. A member of `management_units`, named separately because that set does not say which member is LocalPlane.
             */
            connection_unit?: string | null;
            /**
             * Connection Unit Type
             * @description service | scope | slice — read from systemd, never from the name.
             */
            connection_unit_type?: string | null;
            /** Agent Unit */
            agent_unit: string | null;
            /** Agent Complete */
            agent_complete: boolean;
            /**
             * Agent Unit Type
             * @description The kind of containment the agent has. A `.scope` belongs to a container runtime, which systemd need not publish an effect edge for, so a closure that misses it has proven nothing.
             */
            agent_unit_type?: string | null;
            /** Gaps */
            gaps: string[];
            /** Restart Baseline Invocation Id */
            restart_baseline_invocation_id: string | null;
            runtime_owner?: components["schemas"]["PlanRuntimeOwnerCorrelation"] | null;
        };
        /**
         * PlanValidity
         * @description Whether the published plan still describes what would happen if it ran.
         *
         *     Derived for this request from what LocalPlane has already recorded — never stored, and
         *     reading it changes nothing. A stale plan is not an error and it is not rewritten: the
         *     preview stays exactly as it was published, and the remedy is to plan again.
         */
        PlanValidity: {
            /**
             * State
             * @description current | stale
             */
            state: string;
            /**
             * Reasons
             * @description Empty when current. Otherwise what moved — the plan can no longer be made at all, or it can and comes out different.
             */
            reasons: components["schemas"]["ValidityReason"][];
            /**
             * As Of
             * Format: date-time
             * @description When this was derived. It is never stored.
             */
            as_of: string;
        };
        /**
         * PlanVerification
         * @description What a future verification would have to observe. Nothing observed it.
         */
        PlanVerification: {
            /**
             * Executed
             * @description Always false. No verification has run.
             */
            executed: boolean;
            /** Capability */
            capability: string;
            /** Provider */
            provider: string;
            /** Field */
            field?: string | null;
            /** Expect */
            expect: boolean | number | string;
            /** Condition */
            condition: string;
        };
        /**
         * PlannedChange
         * @description WHAT would change, in one of two shapes.
         *
         *     ``kind`` says which, and it is the field to read first. A **field** change moves one
         *     controlled value to another and fills `field` / `value_type` / `current` / `desired`. An
         *     **action** asks for a declared verb to be carried out and fills `action` /
         *     `observed_state` / `expected_state`; it has no desired *value*, because there is no
         *     retained value for it to reconcile towards.
         *
         *     The unused half is null rather than absent, so the shape of the document does not change
         *     between operations — but nothing outside the half `kind` names carries any meaning.
         */
        PlannedChange: {
            /** Object Id */
            object_id: string;
            /** Object Name */
            object_name: string;
            /**
             * Kind
             * @description field | action
             */
            kind: string;
            /**
             * Field
             * @description Field changes only.
             */
            field?: string | null;
            /**
             * Value Type
             * @description boolean | integer. Field only.
             */
            value_type?: string | null;
            /**
             * Current
             * @description What the newest observation reported. Field changes only.
             */
            current?: boolean | number | null;
            /**
             * Desired
             * @description What the active intent holds. Never supplied. Field changes only.
             */
            desired?: boolean | number | null;
            /**
             * Action
             * @description The declared verb. Actions only, and it comes from the operation the caller named out of a closed vocabulary — never from a value in the request.
             */
            action?: string | null;
            /**
             * Observed State
             * @description The lifecycle state the resource was observed in. Actions only. Not a value to be written over: it is what the operator was looking at when they decided, and for a restart it is part of what a verification is judged against.
             */
            observed_state?: string | null;
            /**
             * Expected State
             * @description The state that must hold afterwards for this to have worked. Actions only.
             */
            expected_state?: string | null;
            /**
             * Expected After
             * @description What the host would read back if this were applied and verified. A distinct question from `desired` — a provider that normalised a value would answer it differently — and equal to it for the operations this build has.
             */
            expected_after: boolean | number | string;
        };
        /**
         * ProtectionReasonView
         * @description One protection reason, evaluated against one object.
         */
        ProtectionReasonView: {
            /** Reason */
            reason: string;
            /**
             * Status
             * @description protected | clear | unknown, for this reason alone.
             */
            status: string;
            /**
             * Detail
             * @description The typed code for what was decided.
             */
            detail: string;
            /** Evidence Id */
            evidence_id: string | null;
            /** Observed At */
            observed_at: string | null;
        };
        /**
         * Provenance
         * @description The whole ownership answer for one object, with every piece of evidence.
         *
         *     A separate resource from the interface because the evidence is heavy and rarely wanted
         *     — but it is available, because a claim about an operator's host that cannot be checked
         *     is worth less than one that can.
         */
        Provenance: {
            /** Object Id */
            object_id: string;
            /** Name */
            name: string;
            /** @description Repeated here to make the independence of the two axes visible. */
            management: components["schemas"]["Management"];
            /** State */
            state: string;
            /** Reason */
            reason: string;
            /** Claims */
            claims: components["schemas"]["OwnershipClaim"][];
            /** Sources */
            sources: components["schemas"]["ConsultedSource"][];
            adoption: components["schemas"]["AdoptionEligibility"];
            /** @description The interface observation the provider evidence was correlated against. */
            observation: components["schemas"]["ObservationRef"] | null;
            /**
             * As Of
             * Format: date-time
             * @description When this assessment was computed. It is never stored.
             */
            as_of: string;
        };
        /**
         * ProviderIssue
         * @description Something a provider could not do during a sweep, named precisely enough to act on.
         */
        ProviderIssue: {
            /**
             * Source
             * @description Which source failed, e.g. rtnetlink.addr.
             */
            source: string;
            /** Code */
            code: string;
            /** Message */
            message: string;
            /** Detail */
            detail?: {
                [key: string]: unknown;
            };
        };
        /**
         * ProviderReadingSummary
         * @description What one provider did during a sweep.
         */
        ProviderReadingSummary: {
            /** Provider */
            provider: string;
            /** Source */
            source: string;
            /** Status */
            status: string;
            /** Reason */
            reason: string | null;
            /** Version */
            version: string | null;
        };
        /** PublishedOwnershipClaim */
        PublishedOwnershipClaim: {
            /**
             * Relation
             * @description created_by | configured_by
             */
            relation: string;
            /** Provider */
            provider: string;
            /** Instance */
            instance: string | null;
            /** Label */
            label: string | null;
            /**
             * Confidence
             * @description confirmed | corroborated
             */
            confidence: string;
            /**
             * External
             * @description Whether this owner is a system LocalPlane would be competing with.
             */
            external: boolean;
        };
        /**
         * ReconcileMtuOperation
         * @description Reconcile a managed interface's runtime MTU to the value its active intent holds.
         *
         *     The body is a type and an object id, and there is nothing else it will accept. There is
         *     no desired value: the target comes from the retained intent, and an operator who wants a
         *     different one revises the intent first — through the endpoint that exists for it, with
         *     the concurrency check, the ownership gate and the version chain that come with it. A Run
         *     that could carry its own target would be a second, weaker route to changing desired
         *     state.
         *
         *     There is also no field, no command, no argv, no provider, no method and no path.
         *     ``extra`` is forbidden on every model here, so a request carrying one is refused at the
         *     edge rather than ignored — and there is nothing behind this that could execute one.
         */
        ReconcileMtuOperation: {
            /**
             * @description The typed operation. The vocabulary is closed; nothing else is accepted. (enum property replaced by openapi-typescript)
             * @enum {string}
             */
            type: "network.interface.reconcile_mtu";
            /**
             * Object Id
             * @description The managed network interface to plan against.
             */
            object_id: string;
        };
        /**
         * Reconciliation
         * @description How a managed object compares with its retained intent, computed now.
         *
         *     Never stored. A reconciliation column would be a second copy of a fact that moves every
         *     time an observation lands, which is exactly the duplicated truth this model avoids.
         */
        Reconciliation: {
            state: components["schemas"]["ReconciliationState"];
            /** Reason */
            reason: string;
            /** Fields */
            fields: components["schemas"]["FieldComparison"][];
            /** @description The observation compared against. null when there has never been one. */
            observation: components["schemas"]["ObservationRef"] | null;
            /**
             * As Of
             * Format: date-time
             * @description When this comparison was computed.
             */
            as_of: string;
        };
        /** ReconciliationResult */
        ReconciliationResult: {
            /** Object Id */
            object_id: string;
            management: components["schemas"]["Management"];
            /** @description null unless the object is managed. An object with no retained intent cannot drift, which is not the same as being in sync. */
            reconciliation: components["schemas"]["Reconciliation"] | null;
            intent: components["schemas"]["IntentSummary"] | null;
        };
        /**
         * ReconciliationState
         * @description How a managed object compares with its retained intent.
         * @enum {string}
         */
        ReconciliationState: "in_sync" | "drifted" | "applying" | "unknown";
        /**
         * RecoveryAttemptView
         * @description One recovery action against a Change: a retry, or a person releasing the hold.
         *
         *     A **later, separate event**. Nothing here is a correction to the Change it belongs to —
         *     that record goes on saying what happened to it, and this says what somebody did about it
         *     afterwards.
         */
        RecoveryAttemptView: {
            /** Attempt Id */
            attempt_id: string;
            /** Sequence */
            sequence: number;
            /**
             * Kind
             * @description retry | resolve.
             */
            kind: string;
            /**
             * Started At
             * Format: date-time
             */
            started_at: string;
            /** Finished At */
            finished_at: string | null;
            /**
             * Outcome
             * @description in_flight | proven | verified | not_written | write_unknown | not_proven | refused | interrupted | resolved. `proven` is a fresh reading establishing the end state with no mutation at all; `refused` is provably no new host effect.
             */
            outcome: string;
            /**
             * Refusal Code
             * @description Why it stopped before dispatching anything. Typed, never prose.
             */
            refusal_code: string | null;
            /** Releases Hold */
            releases_hold: boolean;
            /**
             * Management Path
             * @description The relation this attempt's *own* request proved. Evidence from the original Run is never reused as authority for a new write.
             */
            management_path: string;
            /** Protection Evidence Id */
            protection_evidence_id: string | null;
            /** @description The reading taken **before** anything was written, and what the operation made of it. `verified` here means the end state was already reached. */
            evidence: components["schemas"]["ChangeVerification"];
            /** @description The new write attempt, where one happened. Null where none did. */
            mutation: components["schemas"]["ChangeMutation"] | null;
            /**
             * Host Effect
             * @description What *this attempt* did to the host. The Change's own `host_effect` still answers what the original attempt did; they are separate facts.
             */
            host_effect: string;
            /** @description The reading taken **after** a new write. Null where nothing was written. */
            verification: components["schemas"]["ChangeVerification"] | null;
            /** Confirmation Id */
            confirmation_id: string | null;
            /**
             * Operator Statement
             * @description What a person typed to release the hold deliberately. Resolutions only.
             */
            operator_statement: string | null;
            /** Note */
            note: string | null;
        };
        /**
         * RecoveryAuthority
         * @description An outstanding grant authorising one recovery retry to write again.
         *
         *     Recorded in the same table, under the same single-use rule, as the confirmation that
         *     authorised the original apply — and never that one, which was consumed by it.
         */
        RecoveryAuthority: {
            /** Confirmation Id */
            confirmation_id: string;
            /** Required Method */
            required_method: string;
            /** Method */
            method: string;
            /** Policy */
            policy: string;
            /**
             * Source
             * @description Always `unauthenticated_request`; nobody is identified.
             */
            source: string;
            /**
             * Satisfied At
             * Format: date-time
             */
            satisfied_at: string;
        };
        /**
         * RecoveryConfirmRequest
         * @description Authorise one recovery retry to dispatch a *new* mutation.
         *
         *     The confirmation the original apply consumed authorises nothing here. It authorised an
         *     attempt; that attempt happened. This is a second, separate grant — recorded in the same
         *     table, under the same single-use rule — and at most one may be outstanding at a time.
         *
         *     **A retry does not need this to begin.** A retry first asks whether a fresh reading
         *     already proves the end state, and one that does completes without writing and without
         *     consuming anything. This exists for the case where it does not.
         *
         *     Nothing else is accepted. There is no field here for a value, a verb, a target, a
         *     provider or a command, and ``extra`` is forbidden on every model in this file.
         */
        RecoveryConfirmRequest: {
            /**
             * Acknowledge
             * @description Must be true. The same acknowledge method the original plan required.
             */
            acknowledge: boolean;
            /**
             * Expected Recovery Reason
             * @description Optional cross-check. When given it must equal the change's recovery reason, so authority cannot be granted against a situation the caller has not seen.
             */
            expected_recovery_reason?: string | null;
        };
        /**
         * RecoveryResolveRequest
         * @description Release a recovery hold by hand. No host mutation, and the store refuses one.
         *
         *     This says that a person has inspected or handled the situation and LocalPlane may give
         *     the object back. It does **not** say the change succeeded, that the mutation happened,
         *     that the host is safe, that anything was rolled back or that the intended state was
         *     reached; the original result and the original recovery reason are left saying exactly
         *     what they said, and whatever can currently be observed is recorded beside the decision
         *     as what it is.
         *
         *     ``acknowledge_object`` is the object's name, typed out under the identity this build
         *     holds: an escape from a safety hold should not be one accidental click.
         */
        RecoveryResolveRequest: {
            /**
             * Acknowledge
             * @description Must be true.
             */
            acknowledge: boolean;
            /**
             * Acknowledge Object
             * @description The object's name, typed out. Recorded as the operator's statement; it must match, and it is the deliberation rather than a lookup key.
             */
            acknowledge_object: string;
            /**
             * Note
             * @description What the operator wants recorded beside the release. Kept verbatim.
             */
            note?: string | null;
            /**
             * Expected Recovery Reason
             * @description Optional cross-check against the change's recovery reason.
             */
            expected_recovery_reason?: string | null;
        };
        /** RefreshResult */
        RefreshResult: {
            /** Sweep Id */
            sweep_id: string;
            /** Host Id */
            host_id: string;
            /** Agent Instance Id */
            agent_instance_id: string | null;
            /**
             * Status
             * @description The interface observation's own status. Provider trouble does not change it: a sweep that saw every link and could not reach Docker did the first job.
             */
            status: string;
            /** Object Count */
            object_count: number;
            /** Observation Count */
            observation_count: number;
            /** Missing */
            missing: string[];
            /** Issues */
            issues: components["schemas"]["ProviderIssue"][];
            /**
             * Providers
             * @description One entry per provider consulted for ownership evidence. Empty when none was — an agent without the capability, for instance, which `issues` explains.
             */
            providers?: components["schemas"]["ProviderReadingSummary"][];
        };
        /** RiskFactor */
        RiskFactor: {
            /** Code */
            code: string;
            /**
             * Floor
             * @description The tier this factor sets as a floor.
             */
            floor: string;
            /** Detail */
            detail: string;
        };
        /**
         * RouteEvidenceView
         * @description What the kernel answered when asked for the route to the peer. Facts only.
         *
         *     `oif_index` is the kernel's interface index. The agent does not turn it into a name or
         *     an object: correlating it with a LocalPlane object is a judgement, and judgements are
         *     the backend's.
         */
        RouteEvidenceView: {
            /**
             * Status
             * @description resolved | unreachable | failed | unavailable
             */
            status: string;
            /** Reason */
            reason: string | null;
            /** Family */
            family: string | null;
            /** Destination */
            destination: string | null;
            /** Destination Prefix Length */
            destination_prefix_length: number | null;
            /** Preferred Source */
            preferred_source: string | null;
            /** Gateway */
            gateway: string | null;
            /** Oif Index */
            oif_index: number | null;
            /** Table */
            table: number | null;
            /** Route Type */
            route_type: string | null;
            /** Scope */
            scope: string | null;
            /** Protocol */
            protocol: string | null;
            /** Priority */
            priority: number | null;
            /** Error */
            error: {
                [key: string]: unknown;
            } | null;
        };
        /**
         * Run
         * @description A Run, and the plan it published.
         *
         *     **A Run is still not a Change.** A Run is somebody asking what it would take to
         *     reconcile an object and whether doing so would be safe; a Change is the record that
         *     LocalPlane entered the path on which a host write may occur. They remain separate rows
         *     with separate meanings: planning creates a Run, and only applying creates a Change —
         *     not a preview, not a confirmation and not an armed checkpoint, because none of those
         *     can have moved anything about the host.
         */
        Run: {
            /** Run Id */
            run_id: string;
            /** Host Id */
            host_id: string;
            /** Object Id */
            object_id: string;
            /** Object Name */
            object_name: string;
            /**
             * Operation
             * @description The typed operation this Run plans.
             */
            operation: string;
            /**
             * State
             * @description preview | awaiting_confirmation | arming | applying | verifying | guarded | succeeded | failed | rolling_back | rollback_verifying | rolled_back | recovery_required | cancelled. Thirteen of the run lifecycle's fourteen states, and the store's CHECK accepts exactly these thirteen. `draft` is unreachable because creating a run is planning one. `guarded` means the change was written and verified against the object carrying this operator's own connection and a host-side guard is holding a reversal: the one thing still outstanding is whether the operator can still reach LocalPlane, which only their next request can answer.
             */
            state: string;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /** Cancelled At */
            cancelled_at: string | null;
            /** Finished At */
            finished_at: string | null;
            /**
             * Host Mutated
             * @description True only when the host was written and LocalPlane can prove it. A `write_unknown` change is *not* reported here as a maybe — read `host_effect`.
             */
            host_mutated: boolean;
            /**
             * Host Effect
             * @description none | written | write_unknown. `none` for every run that never crossed the write boundary, and the store refuses any other value for one that did not.
             */
            host_effect: string;
            /**
             * Change Created
             * @description Whether this run crossed the write boundary. Planning, confirming and arming all happen without a Change, because none of them can have moved anything.
             */
            change_created: boolean;
            /** Change Id */
            change_id: string | null;
            /** @description The confirmation recorded for this run, if one has been satisfied. */
            confirmation: components["schemas"]["RunConfirmation"] | null;
            /** @description The recovery material, if it was armed. Its presence is what "recovery is armed" means. */
            checkpoint: components["schemas"]["RunCheckpoint"] | null;
            /** @description The connection guard, if one was established for this run. Its presence is what "a guard is armed" means, and the store refuses a change to the object carrying the management path without one. */
            guard: components["schemas"]["RunGuard"] | null;
            change: components["schemas"]["Change"] | null;
            /**
             * Events
             * @description The append-only transcript, in order. Typed, never a debug log.
             */
            events: components["schemas"]["RunEventView"][];
            /** Note */
            note: string;
            preview: components["schemas"]["RunPreview"];
        };
        /**
         * RunCheckpoint
         * @description The durable material recovery rests on, written before the write boundary.
         *
         *     Its existence *is* what "recovery is armed" means. A previous value known to a running
         *     process is not arming: a process that dies holding it leaves nothing behind, which is
         *     the situation this row exists for.
         */
        RunCheckpoint: {
            /** Checkpoint Id */
            checkpoint_id: string;
            /** Field */
            field: string;
            /**
             * Restores Value
             * @description The verified value before the change.
             */
            restores_value: boolean | number;
            /** Desired Value */
            desired_value: boolean | number;
            /**
             * Observation Id
             * @description The reading the before value came from.
             */
            observation_id: string;
            /**
             * Observed At
             * Format: date-time
             */
            observed_at: string;
            /** Intent Id */
            intent_id: string;
            /** Intent Version */
            intent_version: number;
            /**
             * Management Path
             * @description The target's proven relation to the operator's path at arming time. The store accepts only `not_on_management_path` here: guarded mutation of the path an operator is reached over is a capability this build does not have, so a checkpoint for one cannot be armed at all.
             */
            management_path: string;
            /** Evidence Id */
            evidence_id: string | null;
            /**
             * Execution Correlation
             * @description The stable material the executor needs to reach the target — opaque to the Change engine, which must not learn what kind of thing it is moving.
             */
            execution_correlation: {
                [key: string]: unknown;
            };
            /**
             * Armed At
             * Format: date-time
             */
            armed_at: string;
        };
        /**
         * RunConfirmation
         * @description A confirmation that was actually satisfied. Durable, bound, single-use.
         *
         *     There is no actor and no token. LocalPlane has no authentication, so ``source`` records
         *     the only true thing: an unauthenticated request satisfied the requirement. And nothing
         *     was issued that a caller could present anywhere else — the confirmation is a row naming
         *     this Run and this plan, and the only thing that can use it is an apply of this Run.
         */
        RunConfirmation: {
            /** Confirmation Id */
            confirmation_id: string;
            /** Preview Id */
            preview_id: string;
            /** Preview Digest */
            preview_digest: string;
            /**
             * Required Method
             * @description What the published policy demanded.
             */
            required_method: string;
            /**
             * Method
             * @description What was given.
             */
            method: string;
            /**
             * Typed Statement
             * @description What the operator wrote, when the method was `typed`. Stored as their statement: a record that said only 'typed' would have kept the ceremony and thrown away the evidence.
             */
            typed_statement: string | null;
            /** Policy */
            policy: string;
            /**
             * Source
             * @description `unauthenticated_request`. There is no user model in this build and recording one would be recording a fiction.
             */
            source: string;
            /**
             * Satisfied At
             * Format: date-time
             */
            satisfied_at: string;
            /** Consumed */
            consumed: boolean;
            /** Consumed At */
            consumed_at: string | null;
            /** Consumed By Attempt Id */
            consumed_by_attempt_id: string | null;
        };
        /**
         * RunEventView
         * @description One typed entry in a Run's transcript. Append-only; the store refuses edits.
         */
        RunEventView: {
            /** Sequence */
            sequence: number;
            /**
             * Event
             * @description Closed vocabulary. Branch on this, not on `detail`.
             */
            event: string;
            /** State From */
            state_from: string | null;
            /** State To */
            state_to: string | null;
            /**
             * Occurred At
             * Format: date-time
             */
            occurred_at: string;
            /** Change Id */
            change_id: string | null;
            /**
             * Detail
             * @description Structured accompaniment, never the authoritative statement.
             */
            detail: {
                [key: string]: unknown;
            };
        };
        /**
         * RunGuard
         * @description The connection guard armed for this run: what is holding it, and what it did.
         *
         *     Distinct from `preview.guard`, which says what a guard *would* be. This is the one that
         *     exists. Its `phase` comes from the component actually holding it — the agent on the
         *     host — and is recorded when that component is asked, never inferred from the clock.
         */
        RunGuard: {
            /** Guard Id */
            guard_id: string;
            /**
             * Phase
             * @description arming | armed | disarmed | fired | lost | unreachable. `arming` means LocalPlane asked and has not heard back, which is a crash window rather than a state anything may be dispatched from. `lost` means the holder no longer knows this guard — the change is done and nothing is watching it. `unreachable` means the holder could not be asked, which is the absence of an answer and not an answer.
             */
            phase: string;
            /**
             * Holder Id
             * @description Which instance of the host-side component undertook to hold it. An answer from a different instance is not a report about this guard.
             */
            holder_id: string | null;
            /** Window S */
            window_s: number;
            /** Armed At */
            armed_at: string | null;
            /**
             * Expires At
             * @description The deadline the holder stated, on the holder's clock. Recorded rather than recomputed: that is the clock the reversal will actually run on.
             */
            expires_at: string | null;
            /**
             * Window Lapsed
             * @description Whether the deadline has passed, derived on read and never stored. `true` on an unsettled guard means the reversal has probably happened and nobody has collected its result yet — which is what `POST /runs/{id}/guard/keep` does, and it is why reading this can never move the run on its own.
             */
            window_lapsed: boolean | null;
            /**
             * Restores Value
             * @description What the reversal would put back: the value the checkpoint holds.
             */
            restores_value: boolean | number | null;
            /** Reversal Attempt Id */
            reversal_attempt_id: string;
            /** Kept At */
            kept_at: string | null;
            /**
             * Kept Evidence Id
             * @description The management-path observation that proved this console could still be reached over the changed object. Taken *after* the write, over the path the write could have destroyed — which is why it is a different record from the one the plan rests on.
             */
            kept_evidence_id: string | null;
            /** Settled At */
            settled_at: string | null;
            /** Settled Reason */
            settled_reason: string | null;
            /** Fired At */
            fired_at: string | null;
            /**
             * Reversal Outcome
             * @description not_written | written | write_unknown, for a guard that fired. Never derived from what the target holds afterwards — that answers what the host is now, which is a different question from whether the reversal occurred.
             */
            reversal_outcome: string | null;
            /** Reversal Reason */
            reversal_reason: string | null;
        };
        /** RunList */
        RunList: {
            /** Host Id */
            host_id: string;
            /**
             * State
             * @description The filter this list was produced with.
             */
            state: string;
            /** Count */
            count: number;
            /** Runs */
            runs: components["schemas"]["RunSummary"][];
        };
        /** RunOperation */
        RunOperation: {
            /** Type */
            type: string;
            /** Summary */
            summary: string;
            /** Target Kind */
            target_kind: string;
        };
        /**
         * RunPreview
         * @description One published plan, exactly as it was published. Immutable.
         *
         *     A trigger in the store refuses every update to it, so this is a faithful copy of what
         *     an operator was shown rather than a rendering of what would be decided now. If the
         *     truth underneath it moves, `validity` says so and the remedy is a new Run — the plan is
         *     never rewritten in place under the same identity.
         */
        RunPreview: {
            /** Preview Id */
            preview_id: string;
            /**
             * Preview Digest
             * @description `sha256:<hex>` over a canonical form of the plan. What makes 'the operator confirmed this plan' checkable against 'this is the plan about to run'. Deliberately excludes timestamps and reading identifiers, which move without the plan meaning anything different.
             */
            preview_digest: string;
            /** Digest Version */
            digest_version: number;
            /**
             * Published At
             * Format: date-time
             */
            published_at: string;
            operation: components["schemas"]["RunOperation"];
            what: components["schemas"]["PlannedChange"];
            why: components["schemas"]["PlanRationale"];
            how: components["schemas"]["PlanExecution"];
            evidence: components["schemas"]["PlanEvidence"];
            risk: components["schemas"]["PlanRisk"];
            protection: components["schemas"]["PlanProtection"];
            authorization?: components["schemas"]["PlanAuthorization"] | null;
            systemd_lifecycle_context?: components["schemas"]["PlanSystemdLifecycleContext"] | null;
            self_impact?: components["schemas"]["PlanSelfImpact"] | null;
            confirmation: components["schemas"]["PlanConfirmation"];
            verification: components["schemas"]["PlanVerification"];
            guard: components["schemas"]["PlanGuard"];
            recovery: components["schemas"]["PlanRecovery"];
            validity: components["schemas"]["PlanValidity"];
        };
        /**
         * RunPreviewSummary
         * @description The published plan reduced to what a list needs. The whole of it is on the Run.
         *
         *     Carries the same two shapes as :class:`PlannedChange` and the same ``kind`` to tell them
         *     apart, so a list and a detail view never disagree about what a plan would do.
         */
        RunPreviewSummary: {
            /** Preview Id */
            preview_id: string;
            /** Preview Digest */
            preview_digest: string;
            /**
             * Published At
             * Format: date-time
             */
            published_at: string;
            /**
             * Kind
             * @description field | action
             */
            kind: string;
            /** Field */
            field?: string | null;
            /** Current */
            current?: boolean | number | null;
            /** Desired */
            desired?: boolean | number | null;
            /** Action */
            action?: string | null;
            /** Observed State */
            observed_state?: string | null;
            /** Expected State */
            expected_state?: string | null;
            /** Risk Tier */
            risk_tier: string;
            /** Confirmation Required */
            confirmation_required: boolean;
            /** Execution Availability */
            execution_availability: string;
            /** Execution Eligibility */
            execution_eligibility: string;
            /** Blockers */
            blockers: string[];
            validity: components["schemas"]["PlanValidity"];
        };
        /**
         * RunSummary
         * @description A Run as a list renders it.
         */
        RunSummary: {
            /** Run Id */
            run_id: string;
            /** Host Id */
            host_id: string;
            /** Object Id */
            object_id: string;
            /** Object Name */
            object_name: string;
            /** Operation */
            operation: string;
            /** State */
            state: string;
            /**
             * Created At
             * Format: date-time
             */
            created_at: string;
            /** Cancelled At */
            cancelled_at: string | null;
            /** Finished At */
            finished_at: string | null;
            /** Host Effect */
            host_effect: string;
            /** Change Created */
            change_created: boolean;
            /** Change Id */
            change_id: string | null;
            preview: components["schemas"]["RunPreviewSummary"];
        };
        /**
         * SelfImpactOverrideRequest
         * @description Accept the one typed hazard a self-impact plan publishes. Nothing else.
         *
         *     **Everything this authority is about is already in the preview.** Which unit is at
         *     risk, whether LocalPlane may disappear, and whether it may come back are all in the
         *     immutable document this request names — bound into `expected_preview_digest`, which is
         *     checked. So there is nothing to echo back: asking a client to retype a display name
         *     would turn copied text into authority, and the copy could only ever agree with the
         *     document until the day it did not.
         *
         *     **There is no field for choosing what to bypass.** No `force`, no `ignore_safety`, no
         *     list of blockers. A caller cannot name a hazard, a status or a verdict; the backend
         *     derived whether this exact plan is eligible for this one authority, and the only thing
         *     supplied here is that a person said yes to it.
         */
        SelfImpactOverrideRequest: {
            /**
             * Preview Id
             * @description The preview being overridden. Must be the one this run published.
             */
            preview_id: string;
            /**
             * Expected Preview Digest
             * @description Optional cross-check that the plan you read is the plan you are authorising. Recorded on the grant either way.
             */
            expected_preview_digest?: string | null;
            /**
             * Acknowledge
             * @description Must be true. It records that an operator accepted that carrying this out may interrupt LocalPlane itself, and that no second path to this host has been verified.
             */
            acknowledge: boolean;
        };
        /** Statistics */
        Statistics: {
            /** Rx Bytes */
            rx_bytes: number | null;
            /** Tx Bytes */
            tx_bytes: number | null;
            /** Rx Packets */
            rx_packets: number | null;
            /** Tx Packets */
            tx_packets: number | null;
            /** Rx Errors */
            rx_errors: number | null;
            /** Tx Errors */
            tx_errors: number | null;
            /** Rx Dropped */
            rx_dropped: number | null;
            /** Tx Dropped */
            tx_dropped: number | null;
        };
        /** Sweep */
        Sweep: {
            /** Sweep Id */
            sweep_id: string;
            /** Capability */
            capability: string;
            /**
             * Scope
             * @description inventory | targeted
             */
            scope: string;
            /** Provider */
            provider: string;
            /** Provider Version */
            provider_version: string;
            /**
             * Status
             * @description ok | partial | failed
             */
            status: string;
            /**
             * Started At
             * Format: date-time
             */
            started_at: string;
            /**
             * Completed At
             * Format: date-time
             */
            completed_at: string;
            /**
             * Received At
             * Format: date-time
             */
            received_at: string;
            /** Object Count */
            object_count: number;
            /**
             * Missing
             * @description Requested resource identities the provider authoritatively reported absent; read failures and unobserved inventory members are issues instead.
             */
            missing: string[];
            /** Issues */
            issues: components["schemas"]["ProviderIssue"][];
            /** Agent Instance Id */
            agent_instance_id: string | null;
        };
        /** SweepList */
        SweepList: {
            /** Host Id */
            host_id: string;
            /** Count */
            count: number;
            /** Sweeps */
            sweeps: components["schemas"]["Sweep"][];
        };
        /** SystemdAgentContainment */
        SystemdAgentContainment: {
            /** Status */
            status: string;
            /** Method */
            method: string | null;
            /** Cgroup */
            cgroup: string | null;
            /** Canonical Id */
            canonical_id: string | null;
            /** Invocation Id */
            invocation_id: string | null;
            /** Observed At */
            observed_at: string | null;
            /** Gaps */
            gaps: string[];
            /** Reason */
            reason: string | null;
            /** Detail */
            detail: {
                [key: string]: unknown;
            };
        };
        /** SystemdJob */
        SystemdJob: {
            /** Id */
            id: number;
        };
        /** SystemdMountFacts */
        SystemdMountFacts: {
            /** What */
            what: string | null;
            /** Where */
            where: string | null;
            /** Type */
            type: string | null;
            /** Control Pid */
            control_pid: number | null;
            /** Directory Mode */
            directory_mode: string | null;
            /** Result */
            result: string | null;
            /** Sloppy Options */
            sloppy_options: boolean | null;
            /** Lazy Unmount */
            lazy_unmount: boolean | null;
            /** Force Unmount */
            force_unmount: boolean | null;
            /** Timeout Usec */
            timeout_usec: number | null;
        };
        /** SystemdObservationResult */
        SystemdObservationResult: {
            /** Host Id */
            host_id: string;
            /** Sweep Id */
            sweep_id: string;
            /**
             * Status
             * @description ok | partial | failed
             */
            status: string;
            /** Unit Count */
            unit_count: number;
            /** Issues */
            issues: components["schemas"]["ProviderIssue"][];
            /** Provider Version */
            provider_version: string;
            /** Listed Count */
            listed_count: number | null;
            /** Selected Count */
            selected_count: number | null;
            /** Inventory Limit */
            inventory_limit: number | null;
            /** Inventory Complete */
            inventory_complete: boolean | null;
            /** Truncated */
            truncated: boolean | null;
            /** Cap Reached */
            cap_reached: boolean | null;
            /** Inventory Method */
            inventory_method: string | null;
            agent_unit_resolution: components["schemas"]["SystemdAgentContainment"] | null;
        };
        /** SystemdPathFacts */
        SystemdPathFacts: {
            /** Unit */
            unit: string | null;
            /** Paths */
            paths: components["schemas"]["SystemdWatchedPath"][] | null;
            /** Make Directory */
            make_directory: boolean | null;
            /** Directory Mode */
            directory_mode: string | null;
            /** Result */
            result: string | null;
        };
        /** SystemdRelationship */
        SystemdRelationship: {
            /**
             * Kind
             * @description The exact systemd relation: Requires, Wants, Requisite, BindsTo, PartOf, Before, After, Conflicts, Triggers, TriggeredBy, OnFailure or OnSuccess forms.
             */
            kind: string;
            /**
             * Group
             * @description requirement | ordering | conflict | activation | outcome
             */
            group: string;
            /** Target Unit */
            target_unit: string;
            /** Canonical Target */
            canonical_target: string | null;
            /** Target Object Id */
            target_object_id: string | null;
            /**
             * Resolution
             * @description resolved | referenced | external
             */
            resolution: string;
            /**
             * Estate State
             * @description current | not_observed | null for external
             */
            estate_state: string | null;
            /** Source */
            source: string;
        };
        /** SystemdServiceFacts */
        SystemdServiceFacts: {
            /** Type */
            type: string | null;
            /** Main Pid */
            main_pid: number | null;
            /** Control Pid */
            control_pid: number | null;
            /** Exec Main Pid */
            exec_main_pid: number | null;
            /** Result */
            result: string | null;
            /** Exec Main Code */
            exec_main_code: number | null;
            /** Exec Main Status */
            exec_main_status: number | null;
            /** Restart */
            restart: string | null;
            /** Restart Usec */
            restart_usec: number | null;
            /** N Restarts */
            n_restarts: number | null;
            /** Remain After Exit */
            remain_after_exit: boolean | null;
            /** Guess Main Pid */
            guess_main_pid: boolean | null;
            /** Exec Main Start Timestamp Monotonic */
            exec_main_start_timestamp_monotonic: number | null;
            /** Watchdog Usec */
            watchdog_usec: number | null;
            /** Watchdog Timestamp Monotonic */
            watchdog_timestamp_monotonic: number | null;
            /** Watchdog Signal */
            watchdog_signal: number | null;
            /** Watchdog Pid */
            watchdog_pid: number | null;
            /** Control Group */
            control_group: string | null;
        };
        /**
         * SystemdServiceLifecycleOperation
         * @description Plan one closed systemd service action against an existing LocalPlane object.
         *
         *     The request carries only the generic object id and the operation name.  The canonical
         *     Unit.Id, exact accepted socket tuple, effect graph and authorization assessment are all
         *     derived internally; no unit name or D-Bus field exists in this schema.
         */
        SystemdServiceLifecycleOperation: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "systemd.service.restart" | "systemd.service.start" | "systemd.service.stop";
            /**
             * Object Id
             * @description The existing systemd.unit object to plan against.
             */
            object_id: string;
        };
        /** SystemdSocketFacts */
        SystemdSocketFacts: {
            /** Listen */
            listen: components["schemas"]["SystemdSocketListen"][] | null;
            /** Accept */
            accept: boolean | null;
            /** Accepted */
            accepted: number | null;
            /** Connections */
            connections: number | null;
            /** Refused */
            refused: number | null;
            /** Result */
            result: string | null;
            /** Trigger Limit Interval Usec */
            trigger_limit_interval_usec: number | null;
            /** Trigger Limit Burst */
            trigger_limit_burst: number | null;
        };
        /** SystemdSocketListen */
        SystemdSocketListen: {
            /** Kind */
            kind: string;
            /** Address */
            address: string;
        };
        /** SystemdTimerFacts */
        SystemdTimerFacts: {
            /** Unit */
            unit: string | null;
            /** Next Elapse Usec Realtime */
            next_elapse_usec_realtime: number | null;
            /** Next Elapse Usec Monotonic */
            next_elapse_usec_monotonic: number | null;
            /** Last Trigger Usec */
            last_trigger_usec: number | null;
            /** Last Trigger Usec Monotonic */
            last_trigger_usec_monotonic: number | null;
            /** Persistent */
            persistent: boolean | null;
            /** Randomized Delay Usec */
            randomized_delay_usec: number | null;
            /** Fixed Random Delay */
            fixed_random_delay: boolean | null;
            /** Accuracy Usec */
            accuracy_usec: number | null;
            /** Result */
            result: string | null;
        };
        /**
         * SystemdUnit
         * @description One loaded system-manager unit, identified by systemd's canonical Unit.Id.
         */
        SystemdUnit: {
            /** Object Id */
            object_id: string;
            /** Kind */
            kind: string;
            /** Canonical Id */
            canonical_id: string;
            /** Names */
            names: string[] | null;
            /** Description */
            description: string | null;
            /** Unit Type */
            unit_type: string;
            identity: components["schemas"]["Identity"];
            management: components["schemas"]["Management"];
            health: components["schemas"]["Health"] | null;
            observation: components["schemas"]["Observation"] | null;
            /**
             * Observed In Latest Sweep
             * @description Membership in the latest systemd inventory sweep, independent of a newer targeted observation on this unit.
             */
            observed_in_latest_sweep: boolean | null;
            /** Load State */
            load_state: string | null;
            /** Active State */
            active_state: string | null;
            /** Sub State */
            sub_state: string | null;
            /** Unit File State */
            unit_file_state: string | null;
            /** Unit File Preset */
            unit_file_preset: string | null;
            /** Can Start */
            can_start: boolean | null;
            /** Can Stop */
            can_stop: boolean | null;
            /** Can Reload */
            can_reload: boolean | null;
            /** Refuse Manual Start */
            refuse_manual_start: boolean | null;
            /** Refuse Manual Stop */
            refuse_manual_stop: boolean | null;
            /** Need Daemon Reload */
            need_daemon_reload: boolean | null;
            /** Fragment Path */
            fragment_path: string | null;
            /** Source Path */
            source_path: string | null;
            /** Drop In Paths */
            drop_in_paths: string[] | null;
            /** Transient */
            transient: boolean | null;
            /** Template */
            template: string | null;
            current_job: components["schemas"]["SystemdJob"] | null;
            /**
             * Invocation Id
             * @description Execution-instance evidence. Never part of object identity.
             */
            invocation_id: string | null;
            /** Timestamps */
            timestamps: {
                [key: string]: number | null;
            };
            /** Relationships */
            relationships: components["schemas"]["SystemdRelationship"][];
            service?: components["schemas"]["SystemdServiceFacts"] | null;
            socket?: components["schemas"]["SystemdSocketFacts"] | null;
            timer?: components["schemas"]["SystemdTimerFacts"] | null;
            path?: components["schemas"]["SystemdPathFacts"] | null;
            mount?: components["schemas"]["SystemdMountFacts"] | null;
            agent_process_containment?: components["schemas"]["SystemdAgentContainment"] | null;
            /**
             * First Seen At
             * Format: date-time
             */
            first_seen_at: string;
            /**
             * Last Seen At
             * Format: date-time
             */
            last_seen_at: string;
        };
        /** SystemdUnitList */
        SystemdUnitList: {
            /** Host Id */
            host_id: string;
            /** @description Last recorded systemd observation capability, including unavailable. */
            capability: components["schemas"]["Capability"] | null;
            last_sweep: components["schemas"]["Sweep"] | null;
            /** Count */
            count: number;
            /** Units */
            units: components["schemas"]["SystemdUnit"][];
        };
        /** SystemdWatchedPath */
        SystemdWatchedPath: {
            /** Kind */
            kind: string;
            /** Path */
            path: string;
        };
        /**
         * TransitionResult
         * @description The result of an adopt or a release.
         *
         *     ``host_mutated`` is ``false`` and there is no code path on which it could be anything
         *     else. Adopt records values that were already true on the host; release forgets them.
         *     Neither brings an interface up, changes an MTU, nor dispatches one of the agent's
         *     mutating methods — those belong to the apply of a Run, and no transition here is one.
         */
        TransitionResult: {
            /**
             * Transition
             * @description adopt | release
             */
            transition: string;
            /** Object Id */
            object_id: string;
            /** Host Id */
            host_id: string;
            from_state: components["schemas"]["ManagementState"];
            to_state: components["schemas"]["ManagementState"];
            /**
             * Host Mutated
             * @description Always false. This transition changed LocalPlane's records only.
             */
            host_mutated: boolean;
            /**
             * Host Effect
             * @description Always 'none'.
             */
            host_effect: string;
            /** Note */
            note: string;
            /** Transition Id */
            transition_id: string;
            /**
             * Occurred At
             * Format: date-time
             */
            occurred_at: string;
            intent: components["schemas"]["Intent"];
            /** @description null after a release: nothing is retained, so nothing can disagree. */
            reconciliation: components["schemas"]["Reconciliation"] | null;
            /** @description What LocalPlane knew about who owns this object when it agreed to manage it, including any source it could not consult. Null on release. */
            ownership?: components["schemas"]["Ownership"] | null;
            /**
             * Findings Resolved
             * @description Findings closed by this transition, by id. Closed, never deleted.
             */
            findings_resolved?: string[];
        };
        /**
         * TransportEvidenceView
         * @description What the server side of *this* connection says about how the request arrived.
         *
         *     Read from the accepted socket, never from a header. `X-Forwarded-For`, `X-Real-IP` and
         *     `Forwarded` are not consulted anywhere on this path, and there is no setting that turns
         *     them on: a management path proven from something the caller wrote is a request that has
         *     been believed rather than evidence that has been gathered.
         *
         *     It is transport, not identity. There is no authentication in this product and nobody on
         *     the other end of this socket has been identified — the value of `peer_address` is
         *     precisely that a caller cannot choose it, not that it says who anyone is.
         */
        TransportEvidenceView: {
            /**
             * Peer Address
             * @description The remote end of this connection, when it is a usable unicast address.
             */
            peer_address: string | null;
            /** Peer Family */
            peer_family: string | null;
            /**
             * Local Endpoint Address
             * @description The local address this connection terminated on — the stronger of the two facts, because it is what ties the connection to an object.
             */
            local_endpoint_address: string | null;
            /** Local Endpoint Family */
            local_endpoint_family: string | null;
            /**
             * Usable
             * @description Whether this transport can establish a management path at all.
             */
            usable: boolean;
            /**
             * Reason
             * @description Why it cannot, when it cannot. Loopback is the common one.
             */
            reason: string | null;
        };
        /**
         * TypedValue
         * @description A value with its type stated, so a caller never has to infer one from JSON.
         *
         *     ``true`` and ``1`` are different answers, and a client that guesses will eventually
         *     compare an admin state against an MTU.
         */
        TypedValue: {
            /**
             * Value Type
             * @description boolean | integer
             */
            value_type: string;
            /** Value */
            value: boolean | number;
        };
        /** ValidationError */
        ValidationError: {
            /** Location */
            loc: (string | number)[];
            /** Message */
            msg: string;
            /** Error Type */
            type: string;
        };
        /** ValidityReason */
        ValidityReason: {
            /**
             * Code
             * @description Stable machine-readable code. Branch on this.
             */
            code: string;
            /** Detail */
            detail?: {
                [key: string]: unknown;
            };
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    get_status_api_v1_status_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BackendStatus"];
                };
            };
        };
    };
    get_host_api_v1_host_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Host"];
                };
            };
        };
    };
    get_agent_api_v1_agent_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentStatus"];
                };
            };
        };
    };
    get_agent_capabilities_api_v1_agent_capabilities_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Capabilities"];
                };
            };
        };
    };
    list_network_interfaces_api_v1_network_interfaces_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NetworkInterfaceList"];
                };
            };
        };
    };
    get_network_interface_api_v1_network_interfaces__object_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NetworkInterface"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_network_interface_protection_api_v1_network_interfaces__object_id__protection_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ObjectProtection"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_network_interface_evidence_api_v1_network_interfaces__object_id__evidence_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Evidence"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_network_interface_provenance_api_v1_network_interfaces__object_id__provenance_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Provenance"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    refresh_network_observations_api_v1_network_observations_refresh_post: {
        parameters: {
            query?: {
                /** @description Optional interface names to observe. A name that does not exist on the host is returned in `missing`; nothing is invented for it. */
                names?: string[] | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefreshResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    list_systemd_units_api_v1_systemd_units_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SystemdUnitList"];
                };
            };
        };
    };
    get_systemd_unit_api_v1_systemd_units__object_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SystemdUnit"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    refresh_systemd_observations_api_v1_systemd_observations_refresh_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SystemdObservationResult"];
                };
            };
        };
    };
    list_docker_containers_api_v1_docker_containers_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DockerContainerList"];
                };
            };
        };
    };
    get_docker_container_api_v1_docker_containers__object_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DockerContainer"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    refresh_container_observations_api_v1_docker_containers_observations_refresh_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ContainerObservationResult"];
                };
            };
        };
    };
    read_container_logs_api_v1_docker_containers__object_id__logs_post: {
        parameters: {
            query?: {
                /** @description How many recent lines to return, clamped to the agent's own ceiling. There is no `follow` and no `since`: this is a bounded read, not a stream. */
                tail?: number;
            };
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ContainerLogs"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    read_container_stats_api_v1_docker_containers__object_id__stats_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ContainerStats"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    refresh_management_path_api_v1_management_path_observations_refresh_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ManagementPathObservationResult"];
                };
            };
        };
    };
    get_management_path_api_v1_management_path_get: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ManagementPath"];
                };
            };
        };
    };
    list_sweeps_api_v1_observations_sweeps_get: {
        parameters: {
            query?: {
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SweepList"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    adopt_network_interface_api_v1_network_interfaces__object_id__adopt_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TransitionResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    release_network_interface_api_v1_network_interfaces__object_id__release_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TransitionResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    revise_network_interface_intent_api_v1_network_interfaces__object_id__intent_revise_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExplicitIntentRevisionRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IntentRevisionResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    adopt_runtime_as_network_interface_intent_api_v1_network_interfaces__object_id__intent_adopt_runtime_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["IntentRevisionRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IntentRevisionResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_network_interface_intent_api_v1_network_interfaces__object_id__intent_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Intent"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_network_interface_intent_history_api_v1_network_interfaces__object_id__intent_history_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IntentHistory"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_network_interface_reconciliation_api_v1_network_interfaces__object_id__reconciliation_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                object_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReconciliationResult"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    list_findings_api_v1_findings_get: {
        parameters: {
            query?: {
                /** @description open — currently claimed; resolved — history; all — both. */
                status?: string;
                /** @description Restrict to one object's findings. */
                object_id?: string | null;
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FindingList"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_finding_api_v1_findings__finding_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                finding_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Finding"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    list_runs_api_v1_runs_get: {
        parameters: {
            query?: {
                /** @description preview — Runs still in the preview state; cancelled — Runs cancelled before any write; all — every Run whatever state its lifecycle has reached. Publishing a plan leaves a Run in `preview`; confirming and applying one moves it through the rest of the lifecycle. */
                state?: string;
                /** @description Restrict to one object. */
                object_id?: string | null;
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RunList"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    create_run_api_v1_runs_post: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateRunRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Run"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_run_api_v1_runs__run_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Run"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_run_preview_api_v1_runs__run_id__preview_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RunPreview"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    confirm_run_api_v1_runs__run_id__confirm_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                run_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ConfirmRunRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Run"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    grant_self_impact_override_api_v1_runs__run_id__self_impact_override_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                run_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SelfImpactOverrideRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Run"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    apply_run_api_v1_runs__run_id__apply_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Run"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    keep_guarded_run_api_v1_runs__run_id__guard_keep_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                run_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["KeepGuardedRunRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Run"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    cancel_run_api_v1_runs__run_id__cancel_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Run"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    list_changes_api_v1_changes_get: {
        parameters: {
            query?: {
                /** @description Only changes to this object. */
                object_id?: string | null;
                /** @description in_flight | succeeded | failed | rolled_back | recovery_required. `recovery_required` is the one worth watching. */
                result?: string | null;
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChangeList"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    get_change_api_v1_changes__change_id__get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                change_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Change"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    confirm_change_recovery_api_v1_changes__change_id__recovery_confirm_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                change_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RecoveryConfirmRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Change"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    retry_change_recovery_api_v1_changes__change_id__recovery_retry_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                change_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Change"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
    resolve_change_recovery_api_v1_changes__change_id__recovery_resolve_post: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                change_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RecoveryResolveRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Change"];
                };
            };
            /** @description Validation Error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HTTPValidationError"];
                };
            };
        };
    };
}
